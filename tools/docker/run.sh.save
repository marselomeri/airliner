sudo docker run --rm  \
                -v "${HOME}/.Xauthority:/root/.Xauthority:rw" \
                -v "${HOME}/Desktop/:/home/docker/ \
                --env="DISPLAY" \
                --hostname airliner \
                --user docker \
                --workdir /home/docker \
                --cap-add=SYS_ADMIN \
                --security-opt apparmor:unconfined \
                --net=bridge \
                -p 3000:3000/udp \
                airliner:0.0.1 \
                bash
