/* This file was autogenerated from template version 0.0.0.1 and autogenerator version 0.0.0.1 */

#include "cfe_es_serialization.h"

#ifdef __cplusplus
extern "C" {
#endif

uint32 CFE_ES_AppNameCmd_t_Enc(const CFE_ES_AppNameCmd_t *inObject, char *inOutBuffer, uint32 inSize)
{
	bool status = false;
	pb_ostream_t stream;

	es_app_name_pb es_app_name_msg;
	es_app_name_payload_pb es_app_name_payload_msg;
	CFE_ES_AppNameCmd_Payload_t CFE_ES_AppNameCmd_Payload_t_sub_message = inObject->Payload;
	
	strcpy(es_app_name_payload_msg.Application, CFE_ES_AppNameCmd_Payload_t_sub_message.Application);

	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)&CFE_ES_AppNameCmd_Payload_t_sub_message, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_app_name_payload_pb_fields, &es_app_name_payload_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	es_app_name_msg.Payload = es_app_name_payload_msg;
	
	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)inOutBuffer, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_app_name_pb_fields, &es_app_name_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	return stream.bytes_written;
}

uint32 CFE_ES_AppNameCmd_t_Dec(const char *inBuffer, uint32 inSize, CFE_ES_AppNameCmd_t *inOutObject)
{
	bool status = false;
	pb_istream_t stream;

	es_app_name_pb es_app_name_msg;
	es_app_name_payload_pb es_app_name_payload_msg;
	CFE_ES_AppNameCmd_Payload_t *CFE_ES_AppNameCmd_Payload_t_sub_message;
		
	/* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)inBuffer, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_app_name_pb_fields, &es_app_name_msg); 

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}
	
	CFE_ES_AppNameCmd_Payload_t_sub_message = &inOutObject->Payload;


	/* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)CFE_ES_AppNameCmd_Payload_t_sub_message, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_app_name_payload_pb_fields, &es_app_name_payload_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}
	
	strcpy(CFE_ES_AppNameCmd_Payload_t_sub_message->Application, es_app_name_payload_msg.Application);

	return sizeof(CFE_ES_AppNameCmd_t);
}

uint32 CFE_ES_QueryAllTasksCmd_t_Enc(const CFE_ES_QueryAllTasksCmd_t *inObject, char *inOutBuffer, uint32 inSize)
{
	bool status = false;
	pb_ostream_t stream;

	es_query_all_tasks_pb es_query_all_tasks_msg;
	es_query_all_tasks_payload_pb es_query_all_tasks_payload_msg;
	CFE_ES_QueryAllTasksCmd_Payload_t CFE_ES_QueryAllTasksCmd_Payload_t_sub_message = inObject->Payload;
	
	strcpy(es_query_all_tasks_payload_msg.QueryAllFileName, CFE_ES_QueryAllTasksCmd_Payload_t_sub_message.QueryAllFileName);

	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)&CFE_ES_QueryAllTasksCmd_Payload_t_sub_message, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_query_all_tasks_payload_pb_fields, &es_query_all_tasks_payload_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	es_query_all_tasks_msg.Payload = es_query_all_tasks_payload_msg;
	
	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)inOutBuffer, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_query_all_tasks_pb_fields, &es_query_all_tasks_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	return stream.bytes_written;
}

uint32 CFE_ES_QueryAllTasksCmd_t_Dec(const char *inBuffer, uint32 inSize, CFE_ES_QueryAllTasksCmd_t *inOutObject)
{
	bool status = false;
	pb_istream_t stream;

	es_query_all_tasks_pb es_query_all_tasks_msg;
	es_query_all_tasks_payload_pb es_query_all_tasks_payload_msg;
	CFE_ES_QueryAllTasksCmd_Payload_t *CFE_ES_QueryAllTasksCmd_Payload_t_sub_message;
		
	/* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)inBuffer, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_query_all_tasks_pb_fields, &es_query_all_tasks_msg); 

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}
	
	CFE_ES_QueryAllTasksCmd_Payload_t_sub_message = &inOutObject->Payload;


	/* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)CFE_ES_QueryAllTasksCmd_Payload_t_sub_message, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_query_all_tasks_payload_pb_fields, &es_query_all_tasks_payload_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}
	
	strcpy(CFE_ES_QueryAllTasksCmd_Payload_t_sub_message->QueryAllFileName, es_query_all_tasks_payload_msg.QueryAllFileName);

	return sizeof(CFE_ES_QueryAllTasksCmd_t);
}

uint32 CFE_ES_PerfSetTrigMaskCmd_t_Enc(const CFE_ES_PerfSetTrigMaskCmd_t *inObject, char *inOutBuffer, uint32 inSize)
{
	bool status = false;
	pb_ostream_t stream;

	es_perf_set_trig_mask_pb es_perf_set_trig_mask_msg;
	es_perf_set_trig_mask_payload_pb es_perf_set_trig_mask_payload_msg;
	CFE_ES_PerfSetTrigMaskCmd_Payload_t CFE_ES_PerfSetTrigMaskCmd_Payload_t_sub_message = inObject->Payload;
	
	es_perf_set_trig_mask_payload_msg.TriggerMaskNum = CFE_ES_PerfSetTrigMaskCmd_Payload_t_sub_message.TriggerMaskNum;
	es_perf_set_trig_mask_payload_msg.TriggerMask = CFE_ES_PerfSetTrigMaskCmd_Payload_t_sub_message.TriggerMask;

	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)&CFE_ES_PerfSetTrigMaskCmd_Payload_t_sub_message, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_perf_set_trig_mask_payload_pb_fields, &es_perf_set_trig_mask_payload_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	es_perf_set_trig_mask_msg.Payload = es_perf_set_trig_mask_payload_msg;
	
	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)inOutBuffer, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_perf_set_trig_mask_pb_fields, &es_perf_set_trig_mask_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	return stream.bytes_written;
}

uint32 CFE_ES_PerfSetTrigMaskCmd_t_Dec(const char *inBuffer, uint32 inSize, CFE_ES_PerfSetTrigMaskCmd_t *inOutObject)
{
	bool status = false;
	pb_istream_t stream;

	es_perf_set_trig_mask_pb es_perf_set_trig_mask_msg;
	es_perf_set_trig_mask_payload_pb es_perf_set_trig_mask_payload_msg;
	CFE_ES_PerfSetTrigMaskCmd_Payload_t *CFE_ES_PerfSetTrigMaskCmd_Payload_t_sub_message;
		
	/* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)inBuffer, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_perf_set_trig_mask_pb_fields, &es_perf_set_trig_mask_msg); 

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}
	
	CFE_ES_PerfSetTrigMaskCmd_Payload_t_sub_message = &inOutObject->Payload;


	/* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)CFE_ES_PerfSetTrigMaskCmd_Payload_t_sub_message, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_perf_set_trig_mask_payload_pb_fields, &es_perf_set_trig_mask_payload_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}
	
	CFE_ES_PerfSetTrigMaskCmd_Payload_t_sub_message->TriggerMaskNum = es_perf_set_trig_mask_payload_msg.TriggerMaskNum;
	CFE_ES_PerfSetTrigMaskCmd_Payload_t_sub_message->TriggerMask = es_perf_set_trig_mask_payload_msg.TriggerMask;

	return sizeof(CFE_ES_PerfSetTrigMaskCmd_t);
}

uint32 CFE_ES_ShellCmd_t_Enc(const CFE_ES_ShellCmd_t *inObject, char *inOutBuffer, uint32 inSize)
{
	bool status = false;
	pb_ostream_t stream;

	es_shell_pb es_shell_msg;
	es_shell_payload_pb es_shell_payload_msg;
	CFE_ES_ShellCmd_Payload_t CFE_ES_ShellCmd_Payload_t_sub_message = inObject->Payload;
	
	strcpy(es_shell_payload_msg.CmdString, CFE_ES_ShellCmd_Payload_t_sub_message.CmdString);
	strcpy(es_shell_payload_msg.OutputFilename, CFE_ES_ShellCmd_Payload_t_sub_message.OutputFilename);

	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)&CFE_ES_ShellCmd_Payload_t_sub_message, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_shell_payload_pb_fields, &es_shell_payload_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	es_shell_msg.Payload = es_shell_payload_msg;
	
	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)inOutBuffer, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_shell_pb_fields, &es_shell_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	return stream.bytes_written;
}

uint32 CFE_ES_ShellCmd_t_Dec(const char *inBuffer, uint32 inSize, CFE_ES_ShellCmd_t *inOutObject)
{
	bool status = false;
	pb_istream_t stream;

	es_shell_pb es_shell_msg;
	es_shell_payload_pb es_shell_payload_msg;
	CFE_ES_ShellCmd_Payload_t *CFE_ES_ShellCmd_Payload_t_sub_message;
		
	/* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)inBuffer, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_shell_pb_fields, &es_shell_msg); 

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}
	
	CFE_ES_ShellCmd_Payload_t_sub_message = &inOutObject->Payload;


	/* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)CFE_ES_ShellCmd_Payload_t_sub_message, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_shell_payload_pb_fields, &es_shell_payload_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}
	
	strcpy(CFE_ES_ShellCmd_Payload_t_sub_message->CmdString, es_shell_payload_msg.CmdString);
	strcpy(CFE_ES_ShellCmd_Payload_t_sub_message->OutputFilename, es_shell_payload_msg.OutputFilename);

	return sizeof(CFE_ES_ShellCmd_t);
}

uint32 CFE_ES_PerfStartCmd_t_Enc(const CFE_ES_PerfStartCmd_t *inObject, char *inOutBuffer, uint32 inSize)
{
	bool status = false;
	pb_ostream_t stream;

	es_perf_start_pb es_perf_start_msg;
	es_perf_start_payload_pb es_perf_start_payload_msg;
	CFE_ES_PerfStartCmd_Payload_t CFE_ES_PerfStartCmd_Payload_t_sub_message = inObject->Payload;
	
	es_perf_start_payload_msg.TriggerMode = CFE_ES_PerfStartCmd_Payload_t_sub_message.TriggerMode;

	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)&CFE_ES_PerfStartCmd_Payload_t_sub_message, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_perf_start_payload_pb_fields, &es_perf_start_payload_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	es_perf_start_msg.Payload = es_perf_start_payload_msg;
	
	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)inOutBuffer, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_perf_start_pb_fields, &es_perf_start_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	return stream.bytes_written;
}

uint32 CFE_ES_PerfStartCmd_t_Dec(const char *inBuffer, uint32 inSize, CFE_ES_PerfStartCmd_t *inOutObject)
{
	bool status = false;
	pb_istream_t stream;

	es_perf_start_pb es_perf_start_msg;
	es_perf_start_payload_pb es_perf_start_payload_msg;
	CFE_ES_PerfStartCmd_Payload_t *CFE_ES_PerfStartCmd_Payload_t_sub_message;
		
	/* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)inBuffer, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_perf_start_pb_fields, &es_perf_start_msg); 

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}
	
	CFE_ES_PerfStartCmd_Payload_t_sub_message = &inOutObject->Payload;


	/* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)CFE_ES_PerfStartCmd_Payload_t_sub_message, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_perf_start_payload_pb_fields, &es_perf_start_payload_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}
	
	CFE_ES_PerfStartCmd_Payload_t_sub_message->TriggerMode = es_perf_start_payload_msg.TriggerMode;

	return sizeof(CFE_ES_PerfStartCmd_t);
}

uint32 CFE_ES_StartAppCmd_t_Enc(const CFE_ES_StartAppCmd_t *inObject, char *inOutBuffer, uint32 inSize)
{
	bool status = false;
	pb_ostream_t stream;

	es_start_app_pb es_start_app_msg;
	es_start_payload_pb es_start_payload_msg;
	CFE_ES_StartAppCmd_Payload_t CFE_ES_StartAppCmd_Payload_t_sub_message = inObject->Payload;
	
	strcpy(es_start_payload_msg.AppEntryPoint, CFE_ES_StartAppCmd_Payload_t_sub_message.AppEntryPoint);
	es_start_payload_msg.Priority = CFE_ES_StartAppCmd_Payload_t_sub_message.Priority;
	strcpy(es_start_payload_msg.Application, CFE_ES_StartAppCmd_Payload_t_sub_message.Application);
	strcpy(es_start_payload_msg.AppFileName, CFE_ES_StartAppCmd_Payload_t_sub_message.AppFileName);
	es_start_payload_msg.StackSize = CFE_ES_StartAppCmd_Payload_t_sub_message.StackSize;
	es_start_payload_msg.ExceptionAction = CFE_ES_StartAppCmd_Payload_t_sub_message.ExceptionAction;

	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)&CFE_ES_StartAppCmd_Payload_t_sub_message, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_start_payload_pb_fields, &es_start_payload_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	es_start_app_msg.Payload = es_start_payload_msg;
	
	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)inOutBuffer, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_start_app_pb_fields, &es_start_app_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	return stream.bytes_written;
}

uint32 CFE_ES_StartAppCmd_t_Dec(const char *inBuffer, uint32 inSize, CFE_ES_StartAppCmd_t *inOutObject)
{
	bool status = false;
	pb_istream_t stream;

	es_start_app_pb es_start_app_msg;
	es_start_payload_pb es_start_payload_msg;
	CFE_ES_StartAppCmd_Payload_t *CFE_ES_StartAppCmd_Payload_t_sub_message;
		
	/* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)inBuffer, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_start_app_pb_fields, &es_start_app_msg); 

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}
	
	CFE_ES_StartAppCmd_Payload_t_sub_message = &inOutObject->Payload;


	/* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)CFE_ES_StartAppCmd_Payload_t_sub_message, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_start_payload_pb_fields, &es_start_payload_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}
	
	strcpy(CFE_ES_StartAppCmd_Payload_t_sub_message->AppEntryPoint, es_start_payload_msg.AppEntryPoint);
	CFE_ES_StartAppCmd_Payload_t_sub_message->Priority = es_start_payload_msg.Priority;
	strcpy(CFE_ES_StartAppCmd_Payload_t_sub_message->Application, es_start_payload_msg.Application);
	strcpy(CFE_ES_StartAppCmd_Payload_t_sub_message->AppFileName, es_start_payload_msg.AppFileName);
	CFE_ES_StartAppCmd_Payload_t_sub_message->StackSize = es_start_payload_msg.StackSize;
	CFE_ES_StartAppCmd_Payload_t_sub_message->ExceptionAction = es_start_payload_msg.ExceptionAction;

	return sizeof(CFE_ES_StartAppCmd_t);
}

uint32 CFE_ES_DumpCDSRegCmd_t_Enc(const CFE_ES_DumpCDSRegCmd_t *inObject, char *inOutBuffer, uint32 inSize)
{
	bool status = false;
	pb_ostream_t stream;

	es_dump_cds_reg_pb es_dump_cds_reg_msg;
	es_dump_cds_reg_payload_pb es_dump_cds_reg_payload_msg;
	CFE_ES_DumpCDSRegCmd_Payload_t CFE_ES_DumpCDSRegCmd_Payload_t_sub_message = inObject->Payload;
	
	strcpy(es_dump_cds_reg_payload_msg.DumpFilename, CFE_ES_DumpCDSRegCmd_Payload_t_sub_message.DumpFilename);

	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)&CFE_ES_DumpCDSRegCmd_Payload_t_sub_message, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_dump_cds_reg_payload_pb_fields, &es_dump_cds_reg_payload_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	es_dump_cds_reg_msg.Payload = es_dump_cds_reg_payload_msg;
	
	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)inOutBuffer, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_dump_cds_reg_pb_fields, &es_dump_cds_reg_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	return stream.bytes_written;
}

uint32 CFE_ES_DumpCDSRegCmd_t_Dec(const char *inBuffer, uint32 inSize, CFE_ES_DumpCDSRegCmd_t *inOutObject)
{
	bool status = false;
	pb_istream_t stream;

	es_dump_cds_reg_pb es_dump_cds_reg_msg;
	es_dump_cds_reg_payload_pb es_dump_cds_reg_payload_msg;
	CFE_ES_DumpCDSRegCmd_Payload_t *CFE_ES_DumpCDSRegCmd_Payload_t_sub_message;
		
	/* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)inBuffer, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_dump_cds_reg_pb_fields, &es_dump_cds_reg_msg); 

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}
	
	CFE_ES_DumpCDSRegCmd_Payload_t_sub_message = &inOutObject->Payload;


	/* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)CFE_ES_DumpCDSRegCmd_Payload_t_sub_message, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_dump_cds_reg_payload_pb_fields, &es_dump_cds_reg_payload_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}
	
	strcpy(CFE_ES_DumpCDSRegCmd_Payload_t_sub_message->DumpFilename, es_dump_cds_reg_payload_msg.DumpFilename);

	return sizeof(CFE_ES_DumpCDSRegCmd_t);
}

uint32 CFE_ES_DeleteCDSCmd_t_Enc(const CFE_ES_DeleteCDSCmd_t *inObject, char *inOutBuffer, uint32 inSize)
{
	bool status = false;
	pb_ostream_t stream;

	es_delete_cds_pb es_delete_cds_msg;
	es_delete_cds_payload_pb es_delete_cds_payload_msg;
	CFE_ES_DeleteCDSCmd_Payload_t CFE_ES_DeleteCDSCmd_Payload_t_sub_message = inObject->Payload;
	
	strcpy(es_delete_cds_payload_msg.CdsName, CFE_ES_DeleteCDSCmd_Payload_t_sub_message.CdsName);

	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)&CFE_ES_DeleteCDSCmd_Payload_t_sub_message, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_delete_cds_payload_pb_fields, &es_delete_cds_payload_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	es_delete_cds_msg.Payload = es_delete_cds_payload_msg;
	
	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)inOutBuffer, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_delete_cds_pb_fields, &es_delete_cds_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	return stream.bytes_written;
}

uint32 CFE_ES_DeleteCDSCmd_t_Dec(const char *inBuffer, uint32 inSize, CFE_ES_DeleteCDSCmd_t *inOutObject)
{
	bool status = false;
	pb_istream_t stream;

	es_delete_cds_pb es_delete_cds_msg;
	es_delete_cds_payload_pb es_delete_cds_payload_msg;
	CFE_ES_DeleteCDSCmd_Payload_t *CFE_ES_DeleteCDSCmd_Payload_t_sub_message;
		
	/* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)inBuffer, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_delete_cds_pb_fields, &es_delete_cds_msg); 

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}
	
	CFE_ES_DeleteCDSCmd_Payload_t_sub_message = &inOutObject->Payload;


	/* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)CFE_ES_DeleteCDSCmd_Payload_t_sub_message, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_delete_cds_payload_pb_fields, &es_delete_cds_payload_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}
	
	strcpy(CFE_ES_DeleteCDSCmd_Payload_t_sub_message->CdsName, es_delete_cds_payload_msg.CdsName);

	return sizeof(CFE_ES_DeleteCDSCmd_t);
}

uint32 CFE_ES_AppReloadCmd_t_Enc(const CFE_ES_AppReloadCmd_t *inObject, char *inOutBuffer, uint32 inSize)
{
	bool status = false;
	pb_ostream_t stream;

	es_app_reload_pb es_app_reload_msg;
	es_app_reload_payload_pb es_app_reload_payload_msg;
	CFE_ES_AppReloadCmd_Payload_t CFE_ES_AppReloadCmd_Payload_t_sub_message = inObject->Payload;
	
	strcpy(es_app_reload_payload_msg.Application, CFE_ES_AppReloadCmd_Payload_t_sub_message.Application);
	strcpy(es_app_reload_payload_msg.AppFileName, CFE_ES_AppReloadCmd_Payload_t_sub_message.AppFileName);

	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)&CFE_ES_AppReloadCmd_Payload_t_sub_message, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_app_reload_payload_pb_fields, &es_app_reload_payload_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	es_app_reload_msg.Payload = es_app_reload_payload_msg;
	
	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)inOutBuffer, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_app_reload_pb_fields, &es_app_reload_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	return stream.bytes_written;
}

uint32 CFE_ES_AppReloadCmd_t_Dec(const char *inBuffer, uint32 inSize, CFE_ES_AppReloadCmd_t *inOutObject)
{
	bool status = false;
	pb_istream_t stream;

	es_app_reload_pb es_app_reload_msg;
	es_app_reload_payload_pb es_app_reload_payload_msg;
	CFE_ES_AppReloadCmd_Payload_t *CFE_ES_AppReloadCmd_Payload_t_sub_message;
		
	/* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)inBuffer, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_app_reload_pb_fields, &es_app_reload_msg); 

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}
	
	CFE_ES_AppReloadCmd_Payload_t_sub_message = &inOutObject->Payload;


	/* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)CFE_ES_AppReloadCmd_Payload_t_sub_message, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_app_reload_payload_pb_fields, &es_app_reload_payload_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}
	
	strcpy(CFE_ES_AppReloadCmd_Payload_t_sub_message->Application, es_app_reload_payload_msg.Application);
	strcpy(CFE_ES_AppReloadCmd_Payload_t_sub_message->AppFileName, es_app_reload_payload_msg.AppFileName);

	return sizeof(CFE_ES_AppReloadCmd_t);
}

uint32 CFE_ES_SetMaxPRCountCmd_t_Enc(const CFE_ES_SetMaxPRCountCmd_t *inObject, char *inOutBuffer, uint32 inSize)
{
	bool status = false;
	pb_ostream_t stream;

	es_set_max_pr_count_pb es_set_max_pr_count_msg;
	es_set_max_pr_count_payload_pb es_set_max_pr_count_payload_msg;
	CFE_ES_SetMaxPRCountCmd_Payload_t CFE_ES_SetMaxPRCountCmd_Payload_t_sub_message = inObject->Payload;
	
	es_set_max_pr_count_payload_msg.MaxPRCount = CFE_ES_SetMaxPRCountCmd_Payload_t_sub_message.MaxPRCount;

	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)&CFE_ES_SetMaxPRCountCmd_Payload_t_sub_message, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_set_max_pr_count_payload_pb_fields, &es_set_max_pr_count_payload_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	es_set_max_pr_count_msg.Payload = es_set_max_pr_count_payload_msg;
	
	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)inOutBuffer, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_set_max_pr_count_pb_fields, &es_set_max_pr_count_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	return stream.bytes_written;
}

uint32 CFE_ES_SetMaxPRCountCmd_t_Dec(const char *inBuffer, uint32 inSize, CFE_ES_SetMaxPRCountCmd_t *inOutObject)
{
	bool status = false;
	pb_istream_t stream;

	es_set_max_pr_count_pb es_set_max_pr_count_msg;
	es_set_max_pr_count_payload_pb es_set_max_pr_count_payload_msg;
	CFE_ES_SetMaxPRCountCmd_Payload_t *CFE_ES_SetMaxPRCountCmd_Payload_t_sub_message;
		
	/* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)inBuffer, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_set_max_pr_count_pb_fields, &es_set_max_pr_count_msg); 

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}
	
	CFE_ES_SetMaxPRCountCmd_Payload_t_sub_message = &inOutObject->Payload;


	/* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)CFE_ES_SetMaxPRCountCmd_Payload_t_sub_message, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_set_max_pr_count_payload_pb_fields, &es_set_max_pr_count_payload_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}
	
	CFE_ES_SetMaxPRCountCmd_Payload_t_sub_message->MaxPRCount = es_set_max_pr_count_payload_msg.MaxPRCount;

	return sizeof(CFE_ES_SetMaxPRCountCmd_t);
}

uint32 CFE_ES_TlmPoolStatsCmd_t_Enc(const CFE_ES_TlmPoolStatsCmd_t *inObject, char *inOutBuffer, uint32 inSize)
{
	bool status = false;
	pb_ostream_t stream;

	es_tlm_pool_stats_cmd_pb es_tlm_pool_stats_cmd_msg;
	es_tlm_pool_stats_cmd_payload_pb es_tlm_pool_stats_cmd_payload_msg;
	CFE_ES_TlmPoolStatsCmd_Payload_t CFE_ES_TlmPoolStatsCmd_Payload_t_sub_message = inObject->Payload;
	
	strcpy(es_tlm_pool_stats_cmd_payload_msg.Application, CFE_ES_TlmPoolStatsCmd_Payload_t_sub_message.Application);
	es_tlm_pool_stats_cmd_payload_msg.PoolHandle = CFE_ES_TlmPoolStatsCmd_Payload_t_sub_message.PoolHandle;

	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)&CFE_ES_TlmPoolStatsCmd_Payload_t_sub_message, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_tlm_pool_stats_cmd_payload_pb_fields, &es_tlm_pool_stats_cmd_payload_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	es_tlm_pool_stats_cmd_msg.Payload = es_tlm_pool_stats_cmd_payload_msg;
	
	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)inOutBuffer, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_tlm_pool_stats_cmd_pb_fields, &es_tlm_pool_stats_cmd_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	return stream.bytes_written;
}

uint32 CFE_ES_TlmPoolStatsCmd_t_Dec(const char *inBuffer, uint32 inSize, CFE_ES_TlmPoolStatsCmd_t *inOutObject)
{
	bool status = false;
	pb_istream_t stream;

	es_tlm_pool_stats_cmd_pb es_tlm_pool_stats_cmd_msg;
	es_tlm_pool_stats_cmd_payload_pb es_tlm_pool_stats_cmd_payload_msg;
	CFE_ES_TlmPoolStatsCmd_Payload_t *CFE_ES_TlmPoolStatsCmd_Payload_t_sub_message;
		
	/* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)inBuffer, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_tlm_pool_stats_cmd_pb_fields, &es_tlm_pool_stats_cmd_msg); 

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}
	
	CFE_ES_TlmPoolStatsCmd_Payload_t_sub_message = &inOutObject->Payload;


	/* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)CFE_ES_TlmPoolStatsCmd_Payload_t_sub_message, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_tlm_pool_stats_cmd_payload_pb_fields, &es_tlm_pool_stats_cmd_payload_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}
	
	strcpy(CFE_ES_TlmPoolStatsCmd_Payload_t_sub_message->Application, es_tlm_pool_stats_cmd_payload_msg.Application);
	CFE_ES_TlmPoolStatsCmd_Payload_t_sub_message->PoolHandle = es_tlm_pool_stats_cmd_payload_msg.PoolHandle;

	return sizeof(CFE_ES_TlmPoolStatsCmd_t);
}

uint32 CFE_ES_HkPacket_t_Enc(const CFE_ES_HkPacket_t *inObject, char *inOutBuffer, uint32 inSize)
{
	bool status = false;
	pb_ostream_t stream;

	es_hk_pb es_hk_msg;
	es_hk_payload_pb es_hk_payload_msg;
	CFE_ES_HkPacket_Payload_t CFE_ES_HkPacket_Payload_t_sub_message = inObject->Payload;
	
	es_hk_payload_msg.PerfTriggerMask_count = 4;	
	es_hk_payload_msg.PerfTriggerMask[0] = CFE_ES_HkPacket_Payload_t_sub_message.PerfTriggerMask[0];	
	es_hk_payload_msg.PerfTriggerMask[1] = CFE_ES_HkPacket_Payload_t_sub_message.PerfTriggerMask[1];	
	es_hk_payload_msg.PerfTriggerMask[2] = CFE_ES_HkPacket_Payload_t_sub_message.PerfTriggerMask[2];	
	es_hk_payload_msg.PerfTriggerMask[3] = CFE_ES_HkPacket_Payload_t_sub_message.PerfTriggerMask[3];
	es_hk_payload_msg.ResetSubtype = CFE_ES_HkPacket_Payload_t_sub_message.ResetSubtype;
	es_hk_payload_msg.ProcessorResets = CFE_ES_HkPacket_Payload_t_sub_message.ProcessorResets;
	es_hk_payload_msg.PerfMode = CFE_ES_HkPacket_Payload_t_sub_message.PerfMode;
	es_hk_payload_msg.CFEMinorVersion = CFE_ES_HkPacket_Payload_t_sub_message.CFEMinorVersion;
	es_hk_payload_msg.ErrCounter = CFE_ES_HkPacket_Payload_t_sub_message.ErrCounter;
	es_hk_payload_msg.RegisteredLibs = CFE_ES_HkPacket_Payload_t_sub_message.RegisteredLibs;
	es_hk_payload_msg.CFERevision = CFE_ES_HkPacket_Payload_t_sub_message.CFERevision;
	es_hk_payload_msg.RegisteredExternalApps = CFE_ES_HkPacket_Payload_t_sub_message.RegisteredExternalApps;
	es_hk_payload_msg.RegisteredCoreApps = CFE_ES_HkPacket_Payload_t_sub_message.RegisteredCoreApps;
	es_hk_payload_msg.HeapBytesFree = CFE_ES_HkPacket_Payload_t_sub_message.HeapBytesFree;
	es_hk_payload_msg.SysLogSize = CFE_ES_HkPacket_Payload_t_sub_message.SysLogSize;
	es_hk_payload_msg.PerfFilterMask_count = 4;	
	es_hk_payload_msg.PerfFilterMask[0] = CFE_ES_HkPacket_Payload_t_sub_message.PerfFilterMask[0];	
	es_hk_payload_msg.PerfFilterMask[1] = CFE_ES_HkPacket_Payload_t_sub_message.PerfFilterMask[1];	
	es_hk_payload_msg.PerfFilterMask[2] = CFE_ES_HkPacket_Payload_t_sub_message.PerfFilterMask[2];	
	es_hk_payload_msg.PerfFilterMask[3] = CFE_ES_HkPacket_Payload_t_sub_message.PerfFilterMask[3];
	es_hk_payload_msg.OSALMissionRevision = CFE_ES_HkPacket_Payload_t_sub_message.OSALMissionRevision;
	es_hk_payload_msg.CFECoreChecksum = CFE_ES_HkPacket_Payload_t_sub_message.CFECoreChecksum;
	es_hk_payload_msg.PerfDataStart = CFE_ES_HkPacket_Payload_t_sub_message.PerfDataStart;
	es_hk_payload_msg.BootSource = CFE_ES_HkPacket_Payload_t_sub_message.BootSource;
	es_hk_payload_msg.PerfTriggerCount = CFE_ES_HkPacket_Payload_t_sub_message.PerfTriggerCount;
	es_hk_payload_msg.PerfState = CFE_ES_HkPacket_Payload_t_sub_message.PerfState;
	es_hk_payload_msg.OSALMajorVersion = CFE_ES_HkPacket_Payload_t_sub_message.OSALMajorVersion;
	es_hk_payload_msg.ERLogEntries = CFE_ES_HkPacket_Payload_t_sub_message.ERLogEntries;
	es_hk_payload_msg.SysLogBytesUsed = CFE_ES_HkPacket_Payload_t_sub_message.SysLogBytesUsed;
	es_hk_payload_msg.CFEMissionRevision = CFE_ES_HkPacket_Payload_t_sub_message.CFEMissionRevision;
	es_hk_payload_msg.RegisteredTasks = CFE_ES_HkPacket_Payload_t_sub_message.RegisteredTasks;
	es_hk_payload_msg.OSALMinorVersion = CFE_ES_HkPacket_Payload_t_sub_message.OSALMinorVersion;
	es_hk_payload_msg.CmdCounter = CFE_ES_HkPacket_Payload_t_sub_message.CmdCounter;
	es_hk_payload_msg.HeapMaxBlockSize = CFE_ES_HkPacket_Payload_t_sub_message.HeapMaxBlockSize;
	es_hk_payload_msg.PerfDataEnd = CFE_ES_HkPacket_Payload_t_sub_message.PerfDataEnd;
	es_hk_payload_msg.ERLogIndex = CFE_ES_HkPacket_Payload_t_sub_message.ERLogIndex;
	es_hk_payload_msg.PerfDataToWrite = CFE_ES_HkPacket_Payload_t_sub_message.PerfDataToWrite;
	es_hk_payload_msg.CFEMajorVersion = CFE_ES_HkPacket_Payload_t_sub_message.CFEMajorVersion;
	es_hk_payload_msg.SysLogEntries = CFE_ES_HkPacket_Payload_t_sub_message.SysLogEntries;
	es_hk_payload_msg.OSALRevision = CFE_ES_HkPacket_Payload_t_sub_message.OSALRevision;
	es_hk_payload_msg.MaxProcessorResets = CFE_ES_HkPacket_Payload_t_sub_message.MaxProcessorResets;
	es_hk_payload_msg.HeapBlocksFree = CFE_ES_HkPacket_Payload_t_sub_message.HeapBlocksFree;
	es_hk_payload_msg.SysLogMode = CFE_ES_HkPacket_Payload_t_sub_message.SysLogMode;
	es_hk_payload_msg.ResetType = CFE_ES_HkPacket_Payload_t_sub_message.ResetType;
	es_hk_payload_msg.PerfDataCount = CFE_ES_HkPacket_Payload_t_sub_message.PerfDataCount;

	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)&CFE_ES_HkPacket_Payload_t_sub_message, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_hk_payload_pb_fields, &es_hk_payload_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	es_hk_msg.Payload = es_hk_payload_msg;
	
	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)inOutBuffer, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_hk_pb_fields, &es_hk_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	return stream.bytes_written;
}

uint32 CFE_ES_HkPacket_t_Dec(const char *inBuffer, uint32 inSize, CFE_ES_HkPacket_t *inOutObject)
{
	bool status = false;
	pb_istream_t stream;

	es_hk_pb es_hk_msg;
	es_hk_payload_pb es_hk_payload_msg;
	CFE_ES_HkPacket_Payload_t *CFE_ES_HkPacket_Payload_t_sub_message;
		
	/* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)inBuffer, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_hk_pb_fields, &es_hk_msg); 

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}
	
	CFE_ES_HkPacket_Payload_t_sub_message = &inOutObject->Payload;


	/* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)CFE_ES_HkPacket_Payload_t_sub_message, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_hk_payload_pb_fields, &es_hk_payload_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}
	
	CFE_ES_HkPacket_Payload_t_sub_message->PerfTriggerMask[0] = es_hk_payload_msg.PerfTriggerMask[0];
	CFE_ES_HkPacket_Payload_t_sub_message->PerfTriggerMask[1] = es_hk_payload_msg.PerfTriggerMask[1];
	CFE_ES_HkPacket_Payload_t_sub_message->PerfTriggerMask[2] = es_hk_payload_msg.PerfTriggerMask[2];
	CFE_ES_HkPacket_Payload_t_sub_message->PerfTriggerMask[3] = es_hk_payload_msg.PerfTriggerMask[3];
	CFE_ES_HkPacket_Payload_t_sub_message->ResetSubtype = es_hk_payload_msg.ResetSubtype;
	CFE_ES_HkPacket_Payload_t_sub_message->ProcessorResets = es_hk_payload_msg.ProcessorResets;
	CFE_ES_HkPacket_Payload_t_sub_message->PerfMode = es_hk_payload_msg.PerfMode;
	CFE_ES_HkPacket_Payload_t_sub_message->CFEMinorVersion = es_hk_payload_msg.CFEMinorVersion;
	CFE_ES_HkPacket_Payload_t_sub_message->ErrCounter = es_hk_payload_msg.ErrCounter;
	CFE_ES_HkPacket_Payload_t_sub_message->RegisteredLibs = es_hk_payload_msg.RegisteredLibs;
	CFE_ES_HkPacket_Payload_t_sub_message->CFERevision = es_hk_payload_msg.CFERevision;
	CFE_ES_HkPacket_Payload_t_sub_message->RegisteredExternalApps = es_hk_payload_msg.RegisteredExternalApps;
	CFE_ES_HkPacket_Payload_t_sub_message->RegisteredCoreApps = es_hk_payload_msg.RegisteredCoreApps;
	CFE_ES_HkPacket_Payload_t_sub_message->HeapBytesFree = es_hk_payload_msg.HeapBytesFree;
	CFE_ES_HkPacket_Payload_t_sub_message->SysLogSize = es_hk_payload_msg.SysLogSize;
	CFE_ES_HkPacket_Payload_t_sub_message->PerfFilterMask[0] = es_hk_payload_msg.PerfFilterMask[0];
	CFE_ES_HkPacket_Payload_t_sub_message->PerfFilterMask[1] = es_hk_payload_msg.PerfFilterMask[1];
	CFE_ES_HkPacket_Payload_t_sub_message->PerfFilterMask[2] = es_hk_payload_msg.PerfFilterMask[2];
	CFE_ES_HkPacket_Payload_t_sub_message->PerfFilterMask[3] = es_hk_payload_msg.PerfFilterMask[3];
	CFE_ES_HkPacket_Payload_t_sub_message->OSALMissionRevision = es_hk_payload_msg.OSALMissionRevision;
	CFE_ES_HkPacket_Payload_t_sub_message->CFECoreChecksum = es_hk_payload_msg.CFECoreChecksum;
	CFE_ES_HkPacket_Payload_t_sub_message->PerfDataStart = es_hk_payload_msg.PerfDataStart;
	CFE_ES_HkPacket_Payload_t_sub_message->BootSource = es_hk_payload_msg.BootSource;
	CFE_ES_HkPacket_Payload_t_sub_message->PerfTriggerCount = es_hk_payload_msg.PerfTriggerCount;
	CFE_ES_HkPacket_Payload_t_sub_message->PerfState = es_hk_payload_msg.PerfState;
	CFE_ES_HkPacket_Payload_t_sub_message->OSALMajorVersion = es_hk_payload_msg.OSALMajorVersion;
	CFE_ES_HkPacket_Payload_t_sub_message->ERLogEntries = es_hk_payload_msg.ERLogEntries;
	CFE_ES_HkPacket_Payload_t_sub_message->SysLogBytesUsed = es_hk_payload_msg.SysLogBytesUsed;
	CFE_ES_HkPacket_Payload_t_sub_message->CFEMissionRevision = es_hk_payload_msg.CFEMissionRevision;
	CFE_ES_HkPacket_Payload_t_sub_message->RegisteredTasks = es_hk_payload_msg.RegisteredTasks;
	CFE_ES_HkPacket_Payload_t_sub_message->OSALMinorVersion = es_hk_payload_msg.OSALMinorVersion;
	CFE_ES_HkPacket_Payload_t_sub_message->CmdCounter = es_hk_payload_msg.CmdCounter;
	CFE_ES_HkPacket_Payload_t_sub_message->HeapMaxBlockSize = es_hk_payload_msg.HeapMaxBlockSize;
	CFE_ES_HkPacket_Payload_t_sub_message->PerfDataEnd = es_hk_payload_msg.PerfDataEnd;
	CFE_ES_HkPacket_Payload_t_sub_message->ERLogIndex = es_hk_payload_msg.ERLogIndex;
	CFE_ES_HkPacket_Payload_t_sub_message->PerfDataToWrite = es_hk_payload_msg.PerfDataToWrite;
	CFE_ES_HkPacket_Payload_t_sub_message->CFEMajorVersion = es_hk_payload_msg.CFEMajorVersion;
	CFE_ES_HkPacket_Payload_t_sub_message->SysLogEntries = es_hk_payload_msg.SysLogEntries;
	CFE_ES_HkPacket_Payload_t_sub_message->OSALRevision = es_hk_payload_msg.OSALRevision;
	CFE_ES_HkPacket_Payload_t_sub_message->MaxProcessorResets = es_hk_payload_msg.MaxProcessorResets;
	CFE_ES_HkPacket_Payload_t_sub_message->HeapBlocksFree = es_hk_payload_msg.HeapBlocksFree;
	CFE_ES_HkPacket_Payload_t_sub_message->SysLogMode = es_hk_payload_msg.SysLogMode;
	CFE_ES_HkPacket_Payload_t_sub_message->ResetType = es_hk_payload_msg.ResetType;
	CFE_ES_HkPacket_Payload_t_sub_message->PerfDataCount = es_hk_payload_msg.PerfDataCount;

	return sizeof(CFE_ES_HkPacket_t);
}

uint32 CFE_ES_PerfStopCmd_t_Enc(const CFE_ES_PerfStopCmd_t *inObject, char *inOutBuffer, uint32 inSize)
{
	bool status = false;
	pb_ostream_t stream;

	es_perf_stop_pb es_perf_stop_msg;
	es_perf_stop_payload_pb es_perf_stop_payload_msg;
	CFE_ES_PerfStopCmd_Payload_t CFE_ES_PerfStopCmd_Payload_t_sub_message = inObject->Payload;
	
	strcpy(es_perf_stop_payload_msg.DataFileName, CFE_ES_PerfStopCmd_Payload_t_sub_message.DataFileName);

	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)&CFE_ES_PerfStopCmd_Payload_t_sub_message, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_perf_stop_payload_pb_fields, &es_perf_stop_payload_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	es_perf_stop_msg.Payload = es_perf_stop_payload_msg;
	
	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)inOutBuffer, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_perf_stop_pb_fields, &es_perf_stop_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	return stream.bytes_written;
}

uint32 CFE_ES_PerfStopCmd_t_Dec(const char *inBuffer, uint32 inSize, CFE_ES_PerfStopCmd_t *inOutObject)
{
	bool status = false;
	pb_istream_t stream;

	es_perf_stop_pb es_perf_stop_msg;
	es_perf_stop_payload_pb es_perf_stop_payload_msg;
	CFE_ES_PerfStopCmd_Payload_t *CFE_ES_PerfStopCmd_Payload_t_sub_message;
		
	/* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)inBuffer, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_perf_stop_pb_fields, &es_perf_stop_msg); 

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}
	
	CFE_ES_PerfStopCmd_Payload_t_sub_message = &inOutObject->Payload;


	/* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)CFE_ES_PerfStopCmd_Payload_t_sub_message, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_perf_stop_payload_pb_fields, &es_perf_stop_payload_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}
	
	strcpy(CFE_ES_PerfStopCmd_Payload_t_sub_message->DataFileName, es_perf_stop_payload_msg.DataFileName);

	return sizeof(CFE_ES_PerfStopCmd_t);
}

uint32 CFE_ES_WriteSyslogCmd_t_Enc(const CFE_ES_WriteSyslogCmd_t *inObject, char *inOutBuffer, uint32 inSize)
{
	bool status = false;
	pb_ostream_t stream;

	es_write_sys_log_pb es_write_sys_log_msg;
	es_write_sys_log_payload_pb es_write_sys_log_payload_msg;
	CFE_ES_WriteSyslogCmd_Payload_t CFE_ES_WriteSyslogCmd_Payload_t_sub_message = inObject->Payload;
	
	strcpy(es_write_sys_log_payload_msg.SysLogFileName, CFE_ES_WriteSyslogCmd_Payload_t_sub_message.SysLogFileName);

	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)&CFE_ES_WriteSyslogCmd_Payload_t_sub_message, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_write_sys_log_payload_pb_fields, &es_write_sys_log_payload_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	es_write_sys_log_msg.Payload = es_write_sys_log_payload_msg;
	
	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)inOutBuffer, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_write_sys_log_pb_fields, &es_write_sys_log_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	return stream.bytes_written;
}

uint32 CFE_ES_WriteSyslogCmd_t_Dec(const char *inBuffer, uint32 inSize, CFE_ES_WriteSyslogCmd_t *inOutObject)
{
	bool status = false;
	pb_istream_t stream;

	es_write_sys_log_pb es_write_sys_log_msg;
	es_write_sys_log_payload_pb es_write_sys_log_payload_msg;
	CFE_ES_WriteSyslogCmd_Payload_t *CFE_ES_WriteSyslogCmd_Payload_t_sub_message;
		
	/* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)inBuffer, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_write_sys_log_pb_fields, &es_write_sys_log_msg); 

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}
	
	CFE_ES_WriteSyslogCmd_Payload_t_sub_message = &inOutObject->Payload;


	/* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)CFE_ES_WriteSyslogCmd_Payload_t_sub_message, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_write_sys_log_payload_pb_fields, &es_write_sys_log_payload_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}
	
	strcpy(CFE_ES_WriteSyslogCmd_Payload_t_sub_message->SysLogFileName, es_write_sys_log_payload_msg.SysLogFileName);

	return sizeof(CFE_ES_WriteSyslogCmd_t);
}

uint32 CFE_ES_WriteERlogCmd_t_Enc(const CFE_ES_WriteERlogCmd_t *inObject, char *inOutBuffer, uint32 inSize)
{
	bool status = false;
	pb_ostream_t stream;

	es_write_er_log_pb es_write_er_log_msg;
	es_write_er_log_payload_pb es_write_er_log_payload_msg;
	CFE_ES_WriteERlogCmd_Payload_t CFE_ES_WriteERlogCmd_Payload_t_sub_message = inObject->Payload;
	
	strcpy(es_write_er_log_payload_msg.ERLogFileName, CFE_ES_WriteERlogCmd_Payload_t_sub_message.ERLogFileName);

	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)&CFE_ES_WriteERlogCmd_Payload_t_sub_message, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_write_er_log_payload_pb_fields, &es_write_er_log_payload_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	es_write_er_log_msg.Payload = es_write_er_log_payload_msg;
	
	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)inOutBuffer, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_write_er_log_pb_fields, &es_write_er_log_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	return stream.bytes_written;
}

uint32 CFE_ES_WriteERlogCmd_t_Dec(const char *inBuffer, uint32 inSize, CFE_ES_WriteERlogCmd_t *inOutObject)
{
	bool status = false;
	pb_istream_t stream;

	es_write_er_log_pb es_write_er_log_msg;
	es_write_er_log_payload_pb es_write_er_log_payload_msg;
	CFE_ES_WriteERlogCmd_Payload_t *CFE_ES_WriteERlogCmd_Payload_t_sub_message;
		
	/* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)inBuffer, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_write_er_log_pb_fields, &es_write_er_log_msg); 

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}
	
	CFE_ES_WriteERlogCmd_Payload_t_sub_message = &inOutObject->Payload;


	/* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)CFE_ES_WriteERlogCmd_Payload_t_sub_message, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_write_er_log_payload_pb_fields, &es_write_er_log_payload_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}
	
	strcpy(CFE_ES_WriteERlogCmd_Payload_t_sub_message->ERLogFileName, es_write_er_log_payload_msg.ERLogFileName);

	return sizeof(CFE_ES_WriteERlogCmd_t);
}

uint32 CFE_ES_QueryAllCmd_t_Enc(const CFE_ES_QueryAllCmd_t *inObject, char *inOutBuffer, uint32 inSize)
{
	bool status = false;
	pb_ostream_t stream;

	es_query_all_pb es_query_all_msg;
	es_query_all_payload_pb es_query_all_payload_msg;
	CFE_ES_QueryAllCmd_Payload_t CFE_ES_QueryAllCmd_Payload_t_sub_message = inObject->Payload;
	
	strcpy(es_query_all_payload_msg.QueryAllFileName, CFE_ES_QueryAllCmd_Payload_t_sub_message.QueryAllFileName);

	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)&CFE_ES_QueryAllCmd_Payload_t_sub_message, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_query_all_payload_pb_fields, &es_query_all_payload_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	es_query_all_msg.Payload = es_query_all_payload_msg;
	
	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)inOutBuffer, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_query_all_pb_fields, &es_query_all_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	return stream.bytes_written;
}

uint32 CFE_ES_QueryAllCmd_t_Dec(const char *inBuffer, uint32 inSize, CFE_ES_QueryAllCmd_t *inOutObject)
{
	bool status = false;
	pb_istream_t stream;

	es_query_all_pb es_query_all_msg;
	es_query_all_payload_pb es_query_all_payload_msg;
	CFE_ES_QueryAllCmd_Payload_t *CFE_ES_QueryAllCmd_Payload_t_sub_message;
		
	/* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)inBuffer, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_query_all_pb_fields, &es_query_all_msg); 

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}
	
	CFE_ES_QueryAllCmd_Payload_t_sub_message = &inOutObject->Payload;


	/* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)CFE_ES_QueryAllCmd_Payload_t_sub_message, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_query_all_payload_pb_fields, &es_query_all_payload_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}
	
	strcpy(CFE_ES_QueryAllCmd_Payload_t_sub_message->QueryAllFileName, es_query_all_payload_msg.QueryAllFileName);

	return sizeof(CFE_ES_QueryAllCmd_t);
}

uint32 CFE_ES_OneAppTlm_t_Enc(const CFE_ES_OneAppTlm_t *inObject, char *inOutBuffer, uint32 inSize)
{
	bool status = false;
	pb_ostream_t stream;

	es_one_app_pb es_one_app_msg;
	es_one_app_payload_pb es_one_app_payload_msg;
	CFE_ES_OneAppTlm_Payload_t CFE_ES_OneAppTlm_Payload_t_sub_message = inObject->Payload;
	es_app_info_pb es_app_info_msg;
	CFE_ES_AppInfo_t CFE_ES_AppInfo_t_sub_message = CFE_ES_OneAppTlm_Payload_t_sub_message.AppInfo;
	
	es_app_info_msg.ExecutionCounter = CFE_ES_AppInfo_t_sub_message.ExecutionCounter;
	es_app_info_msg.NumOfChildTasks = CFE_ES_AppInfo_t_sub_message.NumOfChildTasks;
	strcpy(es_app_info_msg.Name, CFE_ES_AppInfo_t_sub_message.Name);
	strcpy(es_app_info_msg.MainTaskName, CFE_ES_AppInfo_t_sub_message.MainTaskName);
	es_app_info_msg.CodeAddress = CFE_ES_AppInfo_t_sub_message.CodeAddress;
	es_app_info_msg.MainTaskId = CFE_ES_AppInfo_t_sub_message.MainTaskId;
	es_app_info_msg.CodeSize = CFE_ES_AppInfo_t_sub_message.CodeSize;
	strcpy(es_app_info_msg.FileName, CFE_ES_AppInfo_t_sub_message.FileName);
	es_app_info_msg.Priority = CFE_ES_AppInfo_t_sub_message.Priority;
	es_app_info_msg.StartAddress = CFE_ES_AppInfo_t_sub_message.StartAddress;
	es_app_info_msg.DataAddress = CFE_ES_AppInfo_t_sub_message.DataAddress;
	strcpy(es_app_info_msg.EntryPoint, CFE_ES_AppInfo_t_sub_message.EntryPoint);
	es_app_info_msg.BSSAddress = CFE_ES_AppInfo_t_sub_message.BSSAddress;
	es_app_info_msg.AppId = CFE_ES_AppInfo_t_sub_message.AppId;
	es_app_info_msg.BSSSize = CFE_ES_AppInfo_t_sub_message.BSSSize;
	es_app_info_msg.AddressesAreValid = CFE_ES_AppInfo_t_sub_message.AddressesAreValid;
	es_app_info_msg.StackSize = CFE_ES_AppInfo_t_sub_message.StackSize;
	es_app_info_msg.DataSize = CFE_ES_AppInfo_t_sub_message.DataSize;
	es_app_info_msg.Type = CFE_ES_AppInfo_t_sub_message.Type;
	es_app_info_msg.ExceptionAction = CFE_ES_AppInfo_t_sub_message.ExceptionAction;
	es_app_info_msg.ModuleId = CFE_ES_AppInfo_t_sub_message.ModuleId;

	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)&CFE_ES_AppInfo_t_sub_message, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_app_info_pb_fields, &es_app_info_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	es_one_app_payload_msg.AppInfo = es_app_info_msg;

	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)&CFE_ES_OneAppTlm_Payload_t_sub_message, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_one_app_payload_pb_fields, &es_one_app_payload_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	es_one_app_msg.Payload = es_one_app_payload_msg;
	
	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)inOutBuffer, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_one_app_pb_fields, &es_one_app_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	return stream.bytes_written;
}

uint32 CFE_ES_OneAppTlm_t_Dec(const char *inBuffer, uint32 inSize, CFE_ES_OneAppTlm_t *inOutObject)
{
	bool status = false;
	pb_istream_t stream;

	es_one_app_pb es_one_app_msg;
	es_one_app_payload_pb es_one_app_payload_msg;
	CFE_ES_OneAppTlm_Payload_t *CFE_ES_OneAppTlm_Payload_t_sub_message;
		
	es_app_info_pb es_app_info_msg;
	CFE_ES_AppInfo_t *CFE_ES_AppInfo_t_sub_message;
		
	/* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)inBuffer, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_one_app_pb_fields, &es_one_app_msg); 

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}
	
	CFE_ES_OneAppTlm_Payload_t_sub_message = &inOutObject->Payload;


	/* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)CFE_ES_OneAppTlm_Payload_t_sub_message, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_one_app_payload_pb_fields, &es_one_app_payload_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}
	
	CFE_ES_AppInfo_t_sub_message = &CFE_ES_OneAppTlm_Payload_t_sub_message->AppInfo;

	/* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)CFE_ES_AppInfo_t_sub_message, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_app_info_pb_fields, &es_app_info_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}
	
	CFE_ES_AppInfo_t_sub_message->ExecutionCounter = es_app_info_msg.ExecutionCounter;
	CFE_ES_AppInfo_t_sub_message->NumOfChildTasks = es_app_info_msg.NumOfChildTasks;
	strcpy(CFE_ES_AppInfo_t_sub_message->Name, es_app_info_msg.Name);
	strcpy(CFE_ES_AppInfo_t_sub_message->MainTaskName, es_app_info_msg.MainTaskName);
	CFE_ES_AppInfo_t_sub_message->CodeAddress = es_app_info_msg.CodeAddress;
	CFE_ES_AppInfo_t_sub_message->MainTaskId = es_app_info_msg.MainTaskId;
	CFE_ES_AppInfo_t_sub_message->CodeSize = es_app_info_msg.CodeSize;
	strcpy(CFE_ES_AppInfo_t_sub_message->FileName, es_app_info_msg.FileName);
	CFE_ES_AppInfo_t_sub_message->Priority = es_app_info_msg.Priority;
	CFE_ES_AppInfo_t_sub_message->StartAddress = es_app_info_msg.StartAddress;
	CFE_ES_AppInfo_t_sub_message->DataAddress = es_app_info_msg.DataAddress;
	strcpy(CFE_ES_AppInfo_t_sub_message->EntryPoint, es_app_info_msg.EntryPoint);
	CFE_ES_AppInfo_t_sub_message->BSSAddress = es_app_info_msg.BSSAddress;
	CFE_ES_AppInfo_t_sub_message->AppId = es_app_info_msg.AppId;
	CFE_ES_AppInfo_t_sub_message->BSSSize = es_app_info_msg.BSSSize;
	CFE_ES_AppInfo_t_sub_message->AddressesAreValid = es_app_info_msg.AddressesAreValid;
	CFE_ES_AppInfo_t_sub_message->StackSize = es_app_info_msg.StackSize;
	CFE_ES_AppInfo_t_sub_message->DataSize = es_app_info_msg.DataSize;
	CFE_ES_AppInfo_t_sub_message->Type = es_app_info_msg.Type;
	CFE_ES_AppInfo_t_sub_message->ExceptionAction = es_app_info_msg.ExceptionAction;
	CFE_ES_AppInfo_t_sub_message->ModuleId = es_app_info_msg.ModuleId;

	return sizeof(CFE_ES_OneAppTlm_t);
}

uint32 CFE_ES_ShellPacket_t_Enc(const CFE_ES_ShellPacket_t *inObject, char *inOutBuffer, uint32 inSize)
{
	bool status = false;
	pb_ostream_t stream;

	es_shell_packet_pb es_shell_packet_msg;
	es_shell_packet_payload_pb es_shell_packet_payload_msg;
	CFE_ES_ShellPacket_Payload_t CFE_ES_ShellPacket_Payload_t_sub_message = inObject->Payload;
	
	strcpy(es_shell_packet_payload_msg.ShellOutput, CFE_ES_ShellPacket_Payload_t_sub_message.ShellOutput);

	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)&CFE_ES_ShellPacket_Payload_t_sub_message, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_shell_packet_payload_pb_fields, &es_shell_packet_payload_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	es_shell_packet_msg.Payload = es_shell_packet_payload_msg;
	
	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)inOutBuffer, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_shell_packet_pb_fields, &es_shell_packet_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	return stream.bytes_written;
}

uint32 CFE_ES_ShellPacket_t_Dec(const char *inBuffer, uint32 inSize, CFE_ES_ShellPacket_t *inOutObject)
{
	bool status = false;
	pb_istream_t stream;

	es_shell_packet_pb es_shell_packet_msg;
	es_shell_packet_payload_pb es_shell_packet_payload_msg;
	CFE_ES_ShellPacket_Payload_t *CFE_ES_ShellPacket_Payload_t_sub_message;
		
	/* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)inBuffer, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_shell_packet_pb_fields, &es_shell_packet_msg); 

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}
	
	CFE_ES_ShellPacket_Payload_t_sub_message = &inOutObject->Payload;


	/* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)CFE_ES_ShellPacket_Payload_t_sub_message, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_shell_packet_payload_pb_fields, &es_shell_packet_payload_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}
	
	strcpy(CFE_ES_ShellPacket_Payload_t_sub_message->ShellOutput, es_shell_packet_payload_msg.ShellOutput);

	return sizeof(CFE_ES_ShellPacket_t);
}

uint32 CFE_ES_RestartCmd_t_Enc(const CFE_ES_RestartCmd_t *inObject, char *inOutBuffer, uint32 inSize)
{
	bool status = false;
	pb_ostream_t stream;

	es_restart_pb es_restart_msg;
	es_restart_payload_pb es_restart_payload_msg;
	CFE_ES_RestartCmd_Payload_t CFE_ES_RestartCmd_Payload_t_sub_message = inObject->Payload;
	
	es_restart_payload_msg.RestartType = CFE_ES_RestartCmd_Payload_t_sub_message.RestartType;

	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)&CFE_ES_RestartCmd_Payload_t_sub_message, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_restart_payload_pb_fields, &es_restart_payload_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	es_restart_msg.Payload = es_restart_payload_msg;
	
	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)inOutBuffer, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_restart_pb_fields, &es_restart_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	return stream.bytes_written;
}

uint32 CFE_ES_RestartCmd_t_Dec(const char *inBuffer, uint32 inSize, CFE_ES_RestartCmd_t *inOutObject)
{
	bool status = false;
	pb_istream_t stream;

	es_restart_pb es_restart_msg;
	es_restart_payload_pb es_restart_payload_msg;
	CFE_ES_RestartCmd_Payload_t *CFE_ES_RestartCmd_Payload_t_sub_message;
		
	/* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)inBuffer, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_restart_pb_fields, &es_restart_msg); 

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}
	
	CFE_ES_RestartCmd_Payload_t_sub_message = &inOutObject->Payload;


	/* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)CFE_ES_RestartCmd_Payload_t_sub_message, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_restart_payload_pb_fields, &es_restart_payload_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}
	
	CFE_ES_RestartCmd_Payload_t_sub_message->RestartType = es_restart_payload_msg.RestartType;

	return sizeof(CFE_ES_RestartCmd_t);
}

uint32 CFE_ES_OverWriteSysLogCmd_t_Enc(const CFE_ES_OverWriteSysLogCmd_t *inObject, char *inOutBuffer, uint32 inSize)
{
	bool status = false;
	pb_ostream_t stream;

	es_overwrite_sys_log_pb es_overwrite_sys_log_msg;
	es_overwrite_sys_log_payload_pb es_overwrite_sys_log_payload_msg;
	CFE_ES_OverWriteSysLogCmd_Payload_t CFE_ES_OverWriteSysLogCmd_Payload_t_sub_message = inObject->Payload;
	
	es_overwrite_sys_log_payload_msg.Mode = CFE_ES_OverWriteSysLogCmd_Payload_t_sub_message.Mode;

	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)&CFE_ES_OverWriteSysLogCmd_Payload_t_sub_message, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_overwrite_sys_log_payload_pb_fields, &es_overwrite_sys_log_payload_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	es_overwrite_sys_log_msg.Payload = es_overwrite_sys_log_payload_msg;
	
	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)inOutBuffer, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_overwrite_sys_log_pb_fields, &es_overwrite_sys_log_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	return stream.bytes_written;
}

uint32 CFE_ES_OverWriteSysLogCmd_t_Dec(const char *inBuffer, uint32 inSize, CFE_ES_OverWriteSysLogCmd_t *inOutObject)
{
	bool status = false;
	pb_istream_t stream;

	es_overwrite_sys_log_pb es_overwrite_sys_log_msg;
	es_overwrite_sys_log_payload_pb es_overwrite_sys_log_payload_msg;
	CFE_ES_OverWriteSysLogCmd_Payload_t *CFE_ES_OverWriteSysLogCmd_Payload_t_sub_message;
		
	/* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)inBuffer, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_overwrite_sys_log_pb_fields, &es_overwrite_sys_log_msg); 

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}
	
	CFE_ES_OverWriteSysLogCmd_Payload_t_sub_message = &inOutObject->Payload;


	/* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)CFE_ES_OverWriteSysLogCmd_Payload_t_sub_message, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_overwrite_sys_log_payload_pb_fields, &es_overwrite_sys_log_payload_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}
	
	CFE_ES_OverWriteSysLogCmd_Payload_t_sub_message->Mode = es_overwrite_sys_log_payload_msg.Mode;

	return sizeof(CFE_ES_OverWriteSysLogCmd_t);
}

uint32 CFE_ES_PerfSetFilterMaskCmd_t_Enc(const CFE_ES_PerfSetFilterMaskCmd_t *inObject, char *inOutBuffer, uint32 inSize)
{
	bool status = false;
	pb_ostream_t stream;

	es_perf_set_filter_mask_pb es_perf_set_filter_mask_msg;
	es_perf_set_filter_mask_payload_pb es_perf_set_filter_mask_payload_msg;
	CFE_ES_PerfSetFilterMaskCmd_Payload_t CFE_ES_PerfSetFilterMaskCmd_Payload_t_sub_message = inObject->Payload;
	
	es_perf_set_filter_mask_payload_msg.FilterMaskNum = CFE_ES_PerfSetFilterMaskCmd_Payload_t_sub_message.FilterMaskNum;
	es_perf_set_filter_mask_payload_msg.FilterMask = CFE_ES_PerfSetFilterMaskCmd_Payload_t_sub_message.FilterMask;

	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)&CFE_ES_PerfSetFilterMaskCmd_Payload_t_sub_message, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_perf_set_filter_mask_payload_pb_fields, &es_perf_set_filter_mask_payload_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	es_perf_set_filter_mask_msg.Payload = es_perf_set_filter_mask_payload_msg;
	
	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)inOutBuffer, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_perf_set_filter_mask_pb_fields, &es_perf_set_filter_mask_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	return stream.bytes_written;
}

uint32 CFE_ES_PerfSetFilterMaskCmd_t_Dec(const char *inBuffer, uint32 inSize, CFE_ES_PerfSetFilterMaskCmd_t *inOutObject)
{
	bool status = false;
	pb_istream_t stream;

	es_perf_set_filter_mask_pb es_perf_set_filter_mask_msg;
	es_perf_set_filter_mask_payload_pb es_perf_set_filter_mask_payload_msg;
	CFE_ES_PerfSetFilterMaskCmd_Payload_t *CFE_ES_PerfSetFilterMaskCmd_Payload_t_sub_message;
		
	/* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)inBuffer, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_perf_set_filter_mask_pb_fields, &es_perf_set_filter_mask_msg); 

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}
	
	CFE_ES_PerfSetFilterMaskCmd_Payload_t_sub_message = &inOutObject->Payload;


	/* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)CFE_ES_PerfSetFilterMaskCmd_Payload_t_sub_message, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_perf_set_filter_mask_payload_pb_fields, &es_perf_set_filter_mask_payload_msg);

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}
	
	CFE_ES_PerfSetFilterMaskCmd_Payload_t_sub_message->FilterMaskNum = es_perf_set_filter_mask_payload_msg.FilterMaskNum;
	CFE_ES_PerfSetFilterMaskCmd_Payload_t_sub_message->FilterMask = es_perf_set_filter_mask_payload_msg.FilterMask;

	return sizeof(CFE_ES_PerfSetFilterMaskCmd_t);
}

#ifdef __cplusplus
} /* extern "C" */
#endif
