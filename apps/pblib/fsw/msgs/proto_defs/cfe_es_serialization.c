/* This file was autogenerated from template version 0.0.0.2 and autogenerator version 0.0.0.1 */

#include "cfe_es_serialization.h"

#ifdef __cplusplus
extern "C" {
#endif

uint32 CFE_ES_AppNameCmd_t_Enc(const CFE_ES_AppNameCmd_t *inObject, char *inOutBuffer, uint32 inSize)
{
	bool status = false;
	pb_ostream_t stream;

	es_app_name_pb pbMsg;

    strcpy(pbMsg.Payload.Application, inObject->Payload.Application);

	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)inOutBuffer, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_app_name_pb_fields, &pbMsg);

	/* Check for errors... */
	if (!status)
	{
        OS_printf("Error encoding msg: %s", PB_GET_ERROR(&stream));
		return 0;
	}

	return stream.bytes_written;
}

uint32 CFE_ES_AppNameCmd_t_Dec(const char *inBuffer, uint32 inSize, CFE_ES_AppNameCmd_t *inOutObject)
{
	bool status = false;
	pb_istream_t stream;

	es_app_name_pb pbMsg;

    /* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)inBuffer, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_app_name_pb_fields, &pbMsg); 

	/* Check for errors... */
	if (!status)
	{
        OS_printf("Error decoding msg: %s", PB_GET_ERROR(&stream));
		return 0;
	}

    strcpy(inOutObject->Payload.Application, pbMsg.Payload.Application);

	return sizeof(CFE_ES_AppNameCmd_t);
}


uint32 CFE_ES_QueryAllTasksCmd_t_Enc(const CFE_ES_QueryAllTasksCmd_t *inObject, char *inOutBuffer, uint32 inSize)
{
	bool status = false;
	pb_ostream_t stream;

	es_query_all_tasks_pb pbMsg;

    strcpy(pbMsg.Payload.QueryAllFileName, inObject->Payload.QueryAllFileName);

	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)inOutBuffer, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_query_all_tasks_pb_fields, &pbMsg);

	/* Check for errors... */
	if (!status)
	{
        OS_printf("Error encoding msg: %s", PB_GET_ERROR(&stream));
		return 0;
	}

	return stream.bytes_written;
}

uint32 CFE_ES_QueryAllTasksCmd_t_Dec(const char *inBuffer, uint32 inSize, CFE_ES_QueryAllTasksCmd_t *inOutObject)
{
	bool status = false;
	pb_istream_t stream;

	es_query_all_tasks_pb pbMsg;

    /* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)inBuffer, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_query_all_tasks_pb_fields, &pbMsg); 

	/* Check for errors... */
	if (!status)
	{
        OS_printf("Error decoding msg: %s", PB_GET_ERROR(&stream));
		return 0;
	}

    strcpy(inOutObject->Payload.QueryAllFileName, pbMsg.Payload.QueryAllFileName);

	return sizeof(CFE_ES_QueryAllTasksCmd_t);
}


uint32 CFE_ES_PerfSetTrigMaskCmd_t_Enc(const CFE_ES_PerfSetTrigMaskCmd_t *inObject, char *inOutBuffer, uint32 inSize)
{
	bool status = false;
	pb_ostream_t stream;

	es_perf_set_trig_mask_pb pbMsg;

    pbMsg.Payload.TriggerMaskNum = inObject->Payload.TriggerMaskNum;
    pbMsg.Payload.TriggerMask = inObject->Payload.TriggerMask;

	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)inOutBuffer, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_perf_set_trig_mask_pb_fields, &pbMsg);

	/* Check for errors... */
	if (!status)
	{
        OS_printf("Error encoding msg: %s", PB_GET_ERROR(&stream));
		return 0;
	}

	return stream.bytes_written;
}

uint32 CFE_ES_PerfSetTrigMaskCmd_t_Dec(const char *inBuffer, uint32 inSize, CFE_ES_PerfSetTrigMaskCmd_t *inOutObject)
{
	bool status = false;
	pb_istream_t stream;

	es_perf_set_trig_mask_pb pbMsg;

    /* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)inBuffer, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_perf_set_trig_mask_pb_fields, &pbMsg); 

	/* Check for errors... */
	if (!status)
	{
        OS_printf("Error decoding msg: %s", PB_GET_ERROR(&stream));
		return 0;
	}

    inOutObject->Payload.TriggerMaskNum = pbMsg.Payload.TriggerMaskNum;
    inOutObject->Payload.TriggerMask = pbMsg.Payload.TriggerMask;

	return sizeof(CFE_ES_PerfSetTrigMaskCmd_t);
}


uint32 CFE_ES_ShellCmd_t_Enc(const CFE_ES_ShellCmd_t *inObject, char *inOutBuffer, uint32 inSize)
{
	bool status = false;
	pb_ostream_t stream;

	es_shell_pb pbMsg;

    strcpy(pbMsg.Payload.CmdString, inObject->Payload.CmdString);
    strcpy(pbMsg.Payload.OutputFilename, inObject->Payload.OutputFilename);

	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)inOutBuffer, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_shell_pb_fields, &pbMsg);

	/* Check for errors... */
	if (!status)
	{
        OS_printf("Error encoding msg: %s", PB_GET_ERROR(&stream));
		return 0;
	}

	return stream.bytes_written;
}

uint32 CFE_ES_ShellCmd_t_Dec(const char *inBuffer, uint32 inSize, CFE_ES_ShellCmd_t *inOutObject)
{
	bool status = false;
	pb_istream_t stream;

	es_shell_pb pbMsg;

    /* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)inBuffer, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_shell_pb_fields, &pbMsg); 

	/* Check for errors... */
	if (!status)
	{
        OS_printf("Error decoding msg: %s", PB_GET_ERROR(&stream));
		return 0;
	}

    strcpy(inOutObject->Payload.CmdString, pbMsg.Payload.CmdString);
    strcpy(inOutObject->Payload.OutputFilename, pbMsg.Payload.OutputFilename);

	return sizeof(CFE_ES_ShellCmd_t);
}


uint32 CFE_ES_PerfStartCmd_t_Enc(const CFE_ES_PerfStartCmd_t *inObject, char *inOutBuffer, uint32 inSize)
{
	bool status = false;
	pb_ostream_t stream;

	es_perf_start_pb pbMsg;

    pbMsg.Payload.TriggerMode = inObject->Payload.TriggerMode;

	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)inOutBuffer, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_perf_start_pb_fields, &pbMsg);

	/* Check for errors... */
	if (!status)
	{
        OS_printf("Error encoding msg: %s", PB_GET_ERROR(&stream));
		return 0;
	}

	return stream.bytes_written;
}

uint32 CFE_ES_PerfStartCmd_t_Dec(const char *inBuffer, uint32 inSize, CFE_ES_PerfStartCmd_t *inOutObject)
{
	bool status = false;
	pb_istream_t stream;

	es_perf_start_pb pbMsg;

    /* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)inBuffer, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_perf_start_pb_fields, &pbMsg); 

	/* Check for errors... */
	if (!status)
	{
        OS_printf("Error decoding msg: %s", PB_GET_ERROR(&stream));
		return 0;
	}

    inOutObject->Payload.TriggerMode = pbMsg.Payload.TriggerMode;

	return sizeof(CFE_ES_PerfStartCmd_t);
}


uint32 CFE_ES_StartAppCmd_t_Enc(const CFE_ES_StartAppCmd_t *inObject, char *inOutBuffer, uint32 inSize)
{
	bool status = false;
	pb_ostream_t stream;

	es_start_app_pb pbMsg;

    strcpy(pbMsg.Payload.AppEntryPoint, inObject->Payload.AppEntryPoint);
    pbMsg.Payload.Priority = inObject->Payload.Priority;
    strcpy(pbMsg.Payload.Application, inObject->Payload.Application);
    strcpy(pbMsg.Payload.AppFileName, inObject->Payload.AppFileName);
    pbMsg.Payload.StackSize = inObject->Payload.StackSize;
    pbMsg.Payload.ExceptionAction = inObject->Payload.ExceptionAction;

	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)inOutBuffer, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_start_app_pb_fields, &pbMsg);

	/* Check for errors... */
	if (!status)
	{
        OS_printf("Error encoding msg: %s", PB_GET_ERROR(&stream));
		return 0;
	}

	return stream.bytes_written;
}

uint32 CFE_ES_StartAppCmd_t_Dec(const char *inBuffer, uint32 inSize, CFE_ES_StartAppCmd_t *inOutObject)
{
	bool status = false;
	pb_istream_t stream;

	es_start_app_pb pbMsg;

    /* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)inBuffer, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_start_app_pb_fields, &pbMsg); 

	/* Check for errors... */
	if (!status)
	{
        OS_printf("Error decoding msg: %s", PB_GET_ERROR(&stream));
		return 0;
	}

    strcpy(inOutObject->Payload.AppEntryPoint, pbMsg.Payload.AppEntryPoint);
    inOutObject->Payload.Priority = pbMsg.Payload.Priority;
    strcpy(inOutObject->Payload.Application, pbMsg.Payload.Application);
    strcpy(inOutObject->Payload.AppFileName, pbMsg.Payload.AppFileName);
    inOutObject->Payload.StackSize = pbMsg.Payload.StackSize;
    inOutObject->Payload.ExceptionAction = pbMsg.Payload.ExceptionAction;

	return sizeof(CFE_ES_StartAppCmd_t);
}


uint32 CFE_ES_DumpCDSRegCmd_t_Enc(const CFE_ES_DumpCDSRegCmd_t *inObject, char *inOutBuffer, uint32 inSize)
{
	bool status = false;
	pb_ostream_t stream;

	es_dump_cds_reg_pb pbMsg;

    strcpy(pbMsg.Payload.DumpFilename, inObject->Payload.DumpFilename);

	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)inOutBuffer, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_dump_cds_reg_pb_fields, &pbMsg);

	/* Check for errors... */
	if (!status)
	{
        OS_printf("Error encoding msg: %s", PB_GET_ERROR(&stream));
		return 0;
	}

	return stream.bytes_written;
}

uint32 CFE_ES_DumpCDSRegCmd_t_Dec(const char *inBuffer, uint32 inSize, CFE_ES_DumpCDSRegCmd_t *inOutObject)
{
	bool status = false;
	pb_istream_t stream;

	es_dump_cds_reg_pb pbMsg;

    /* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)inBuffer, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_dump_cds_reg_pb_fields, &pbMsg); 

	/* Check for errors... */
	if (!status)
	{
        OS_printf("Error decoding msg: %s", PB_GET_ERROR(&stream));
		return 0;
	}

    strcpy(inOutObject->Payload.DumpFilename, pbMsg.Payload.DumpFilename);

	return sizeof(CFE_ES_DumpCDSRegCmd_t);
}


uint32 CFE_ES_DeleteCDSCmd_t_Enc(const CFE_ES_DeleteCDSCmd_t *inObject, char *inOutBuffer, uint32 inSize)
{
	bool status = false;
	pb_ostream_t stream;

	es_delete_cds_pb pbMsg;

    strcpy(pbMsg.Payload.CdsName, inObject->Payload.CdsName);

	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)inOutBuffer, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_delete_cds_pb_fields, &pbMsg);

	/* Check for errors... */
	if (!status)
	{
        OS_printf("Error encoding msg: %s", PB_GET_ERROR(&stream));
		return 0;
	}

	return stream.bytes_written;
}

uint32 CFE_ES_DeleteCDSCmd_t_Dec(const char *inBuffer, uint32 inSize, CFE_ES_DeleteCDSCmd_t *inOutObject)
{
	bool status = false;
	pb_istream_t stream;

	es_delete_cds_pb pbMsg;

    /* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)inBuffer, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_delete_cds_pb_fields, &pbMsg); 

	/* Check for errors... */
	if (!status)
	{
        OS_printf("Error decoding msg: %s", PB_GET_ERROR(&stream));
		return 0;
	}

    strcpy(inOutObject->Payload.CdsName, pbMsg.Payload.CdsName);

	return sizeof(CFE_ES_DeleteCDSCmd_t);
}


uint32 CFE_ES_AppReloadCmd_t_Enc(const CFE_ES_AppReloadCmd_t *inObject, char *inOutBuffer, uint32 inSize)
{
	bool status = false;
	pb_ostream_t stream;

	es_app_reload_pb pbMsg;

    strcpy(pbMsg.Payload.Application, inObject->Payload.Application);
    strcpy(pbMsg.Payload.AppFileName, inObject->Payload.AppFileName);

	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)inOutBuffer, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_app_reload_pb_fields, &pbMsg);

	/* Check for errors... */
	if (!status)
	{
        OS_printf("Error encoding msg: %s", PB_GET_ERROR(&stream));
		return 0;
	}

	return stream.bytes_written;
}

uint32 CFE_ES_AppReloadCmd_t_Dec(const char *inBuffer, uint32 inSize, CFE_ES_AppReloadCmd_t *inOutObject)
{
	bool status = false;
	pb_istream_t stream;

	es_app_reload_pb pbMsg;

    /* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)inBuffer, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_app_reload_pb_fields, &pbMsg); 

	/* Check for errors... */
	if (!status)
	{
        OS_printf("Error decoding msg: %s", PB_GET_ERROR(&stream));
		return 0;
	}

    strcpy(inOutObject->Payload.Application, pbMsg.Payload.Application);
    strcpy(inOutObject->Payload.AppFileName, pbMsg.Payload.AppFileName);

	return sizeof(CFE_ES_AppReloadCmd_t);
}


uint32 CFE_ES_SetMaxPRCountCmd_t_Enc(const CFE_ES_SetMaxPRCountCmd_t *inObject, char *inOutBuffer, uint32 inSize)
{
	bool status = false;
	pb_ostream_t stream;

	es_set_max_pr_count_pb pbMsg;

    pbMsg.Payload.MaxPRCount = inObject->Payload.MaxPRCount;

	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)inOutBuffer, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_set_max_pr_count_pb_fields, &pbMsg);

	/* Check for errors... */
	if (!status)
	{
        OS_printf("Error encoding msg: %s", PB_GET_ERROR(&stream));
		return 0;
	}

	return stream.bytes_written;
}

uint32 CFE_ES_SetMaxPRCountCmd_t_Dec(const char *inBuffer, uint32 inSize, CFE_ES_SetMaxPRCountCmd_t *inOutObject)
{
	bool status = false;
	pb_istream_t stream;

	es_set_max_pr_count_pb pbMsg;

    /* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)inBuffer, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_set_max_pr_count_pb_fields, &pbMsg); 

	/* Check for errors... */
	if (!status)
	{
        OS_printf("Error decoding msg: %s", PB_GET_ERROR(&stream));
		return 0;
	}

    inOutObject->Payload.MaxPRCount = pbMsg.Payload.MaxPRCount;

	return sizeof(CFE_ES_SetMaxPRCountCmd_t);
}


uint32 CFE_ES_TlmPoolStatsCmd_t_Enc(const CFE_ES_TlmPoolStatsCmd_t *inObject, char *inOutBuffer, uint32 inSize)
{
	bool status = false;
	pb_ostream_t stream;

	es_tlm_pool_stats_cmd_pb pbMsg;

    strcpy(pbMsg.Payload.Application, inObject->Payload.Application);
    pbMsg.Payload.PoolHandle = inObject->Payload.PoolHandle;

	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)inOutBuffer, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_tlm_pool_stats_cmd_pb_fields, &pbMsg);

	/* Check for errors... */
	if (!status)
	{
        OS_printf("Error encoding msg: %s", PB_GET_ERROR(&stream));
		return 0;
	}

	return stream.bytes_written;
}

uint32 CFE_ES_TlmPoolStatsCmd_t_Dec(const char *inBuffer, uint32 inSize, CFE_ES_TlmPoolStatsCmd_t *inOutObject)
{
	bool status = false;
	pb_istream_t stream;

	es_tlm_pool_stats_cmd_pb pbMsg;

    /* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)inBuffer, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_tlm_pool_stats_cmd_pb_fields, &pbMsg); 

	/* Check for errors... */
	if (!status)
	{
        OS_printf("Error decoding msg: %s", PB_GET_ERROR(&stream));
		return 0;
	}

    strcpy(inOutObject->Payload.Application, pbMsg.Payload.Application);
    inOutObject->Payload.PoolHandle = pbMsg.Payload.PoolHandle;

	return sizeof(CFE_ES_TlmPoolStatsCmd_t);
}


uint32 CFE_ES_HkPacket_t_Enc(const CFE_ES_HkPacket_t *inObject, char *inOutBuffer, uint32 inSize)
{
	bool status = false;
	pb_ostream_t stream;

	es_hk_pb pbMsg;

    /* Unroll indexes */
    pbMsg.Payload.PerfTriggerMask_count = 4;
    pbMsg.Payload.PerfTriggerMask[0] = inObject->Payload.PerfTriggerMask[0];
    pbMsg.Payload.PerfTriggerMask[1] = inObject->Payload.PerfTriggerMask[1];
    pbMsg.Payload.PerfTriggerMask[2] = inObject->Payload.PerfTriggerMask[2];
    pbMsg.Payload.PerfTriggerMask[3] = inObject->Payload.PerfTriggerMask[3];
    pbMsg.Payload.ResetSubtype = inObject->Payload.ResetSubtype;
    pbMsg.Payload.ProcessorResets = inObject->Payload.ProcessorResets;
    pbMsg.Payload.PerfMode = inObject->Payload.PerfMode;
    pbMsg.Payload.CFEMinorVersion = inObject->Payload.CFEMinorVersion;
    pbMsg.Payload.ErrCounter = inObject->Payload.ErrCounter;
    pbMsg.Payload.RegisteredLibs = inObject->Payload.RegisteredLibs;
    pbMsg.Payload.CFERevision = inObject->Payload.CFERevision;
    pbMsg.Payload.RegisteredExternalApps = inObject->Payload.RegisteredExternalApps;
    pbMsg.Payload.RegisteredCoreApps = inObject->Payload.RegisteredCoreApps;
    pbMsg.Payload.HeapBytesFree = inObject->Payload.HeapBytesFree;
    pbMsg.Payload.SysLogSize = inObject->Payload.SysLogSize;
    /* Unroll indexes */
    pbMsg.Payload.PerfFilterMask_count = 4;
    pbMsg.Payload.PerfFilterMask[0] = inObject->Payload.PerfFilterMask[0];
    pbMsg.Payload.PerfFilterMask[1] = inObject->Payload.PerfFilterMask[1];
    pbMsg.Payload.PerfFilterMask[2] = inObject->Payload.PerfFilterMask[2];
    pbMsg.Payload.PerfFilterMask[3] = inObject->Payload.PerfFilterMask[3];
    pbMsg.Payload.OSALMissionRevision = inObject->Payload.OSALMissionRevision;
    pbMsg.Payload.CFECoreChecksum = inObject->Payload.CFECoreChecksum;
    pbMsg.Payload.PerfDataStart = inObject->Payload.PerfDataStart;
    pbMsg.Payload.BootSource = inObject->Payload.BootSource;
    pbMsg.Payload.PerfTriggerCount = inObject->Payload.PerfTriggerCount;
    pbMsg.Payload.PerfState = inObject->Payload.PerfState;
    pbMsg.Payload.OSALMajorVersion = inObject->Payload.OSALMajorVersion;
    pbMsg.Payload.ERLogEntries = inObject->Payload.ERLogEntries;
    pbMsg.Payload.SysLogBytesUsed = inObject->Payload.SysLogBytesUsed;
    pbMsg.Payload.CFEMissionRevision = inObject->Payload.CFEMissionRevision;
    pbMsg.Payload.RegisteredTasks = inObject->Payload.RegisteredTasks;
    pbMsg.Payload.OSALMinorVersion = inObject->Payload.OSALMinorVersion;
    pbMsg.Payload.CmdCounter = inObject->Payload.CmdCounter;
    pbMsg.Payload.HeapMaxBlockSize = inObject->Payload.HeapMaxBlockSize;
    pbMsg.Payload.PerfDataEnd = inObject->Payload.PerfDataEnd;
    pbMsg.Payload.ERLogIndex = inObject->Payload.ERLogIndex;
    pbMsg.Payload.PerfDataToWrite = inObject->Payload.PerfDataToWrite;
    pbMsg.Payload.CFEMajorVersion = inObject->Payload.CFEMajorVersion;
    pbMsg.Payload.SysLogEntries = inObject->Payload.SysLogEntries;
    pbMsg.Payload.OSALRevision = inObject->Payload.OSALRevision;
    pbMsg.Payload.MaxProcessorResets = inObject->Payload.MaxProcessorResets;
    pbMsg.Payload.HeapBlocksFree = inObject->Payload.HeapBlocksFree;
    pbMsg.Payload.SysLogMode = inObject->Payload.SysLogMode;
    pbMsg.Payload.ResetType = inObject->Payload.ResetType;
    pbMsg.Payload.PerfDataCount = inObject->Payload.PerfDataCount;

	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)inOutBuffer, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_hk_pb_fields, &pbMsg);

	/* Check for errors... */
	if (!status)
	{
        OS_printf("Error encoding msg: %s", PB_GET_ERROR(&stream));
		return 0;
	}

	return stream.bytes_written;
}

uint32 CFE_ES_HkPacket_t_Dec(const char *inBuffer, uint32 inSize, CFE_ES_HkPacket_t *inOutObject)
{
	bool status = false;
	pb_istream_t stream;

	es_hk_pb pbMsg;

    /* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)inBuffer, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_hk_pb_fields, &pbMsg); 

	/* Check for errors... */
	if (!status)
	{
        OS_printf("Error decoding msg: %s", PB_GET_ERROR(&stream));
		return 0;
	}

    /* Unroll indexes */
    inOutObject->Payload.PerfTriggerMask[0] = pbMsg.Payload.PerfTriggerMask[0];
    inOutObject->Payload.PerfTriggerMask[1] = pbMsg.Payload.PerfTriggerMask[1];
    inOutObject->Payload.PerfTriggerMask[2] = pbMsg.Payload.PerfTriggerMask[2];
    inOutObject->Payload.PerfTriggerMask[3] = pbMsg.Payload.PerfTriggerMask[3];
    inOutObject->Payload.ResetSubtype = pbMsg.Payload.ResetSubtype;
    inOutObject->Payload.ProcessorResets = pbMsg.Payload.ProcessorResets;
    inOutObject->Payload.PerfMode = pbMsg.Payload.PerfMode;
    inOutObject->Payload.CFEMinorVersion = pbMsg.Payload.CFEMinorVersion;
    inOutObject->Payload.ErrCounter = pbMsg.Payload.ErrCounter;
    inOutObject->Payload.RegisteredLibs = pbMsg.Payload.RegisteredLibs;
    inOutObject->Payload.CFERevision = pbMsg.Payload.CFERevision;
    inOutObject->Payload.RegisteredExternalApps = pbMsg.Payload.RegisteredExternalApps;
    inOutObject->Payload.RegisteredCoreApps = pbMsg.Payload.RegisteredCoreApps;
    inOutObject->Payload.HeapBytesFree = pbMsg.Payload.HeapBytesFree;
    inOutObject->Payload.SysLogSize = pbMsg.Payload.SysLogSize;
    /* Unroll indexes */
    inOutObject->Payload.PerfFilterMask[0] = pbMsg.Payload.PerfFilterMask[0];
    inOutObject->Payload.PerfFilterMask[1] = pbMsg.Payload.PerfFilterMask[1];
    inOutObject->Payload.PerfFilterMask[2] = pbMsg.Payload.PerfFilterMask[2];
    inOutObject->Payload.PerfFilterMask[3] = pbMsg.Payload.PerfFilterMask[3];
    inOutObject->Payload.OSALMissionRevision = pbMsg.Payload.OSALMissionRevision;
    inOutObject->Payload.CFECoreChecksum = pbMsg.Payload.CFECoreChecksum;
    inOutObject->Payload.PerfDataStart = pbMsg.Payload.PerfDataStart;
    inOutObject->Payload.BootSource = pbMsg.Payload.BootSource;
    inOutObject->Payload.PerfTriggerCount = pbMsg.Payload.PerfTriggerCount;
    inOutObject->Payload.PerfState = pbMsg.Payload.PerfState;
    inOutObject->Payload.OSALMajorVersion = pbMsg.Payload.OSALMajorVersion;
    inOutObject->Payload.ERLogEntries = pbMsg.Payload.ERLogEntries;
    inOutObject->Payload.SysLogBytesUsed = pbMsg.Payload.SysLogBytesUsed;
    inOutObject->Payload.CFEMissionRevision = pbMsg.Payload.CFEMissionRevision;
    inOutObject->Payload.RegisteredTasks = pbMsg.Payload.RegisteredTasks;
    inOutObject->Payload.OSALMinorVersion = pbMsg.Payload.OSALMinorVersion;
    inOutObject->Payload.CmdCounter = pbMsg.Payload.CmdCounter;
    inOutObject->Payload.HeapMaxBlockSize = pbMsg.Payload.HeapMaxBlockSize;
    inOutObject->Payload.PerfDataEnd = pbMsg.Payload.PerfDataEnd;
    inOutObject->Payload.ERLogIndex = pbMsg.Payload.ERLogIndex;
    inOutObject->Payload.PerfDataToWrite = pbMsg.Payload.PerfDataToWrite;
    inOutObject->Payload.CFEMajorVersion = pbMsg.Payload.CFEMajorVersion;
    inOutObject->Payload.SysLogEntries = pbMsg.Payload.SysLogEntries;
    inOutObject->Payload.OSALRevision = pbMsg.Payload.OSALRevision;
    inOutObject->Payload.MaxProcessorResets = pbMsg.Payload.MaxProcessorResets;
    inOutObject->Payload.HeapBlocksFree = pbMsg.Payload.HeapBlocksFree;
    inOutObject->Payload.SysLogMode = pbMsg.Payload.SysLogMode;
    inOutObject->Payload.ResetType = pbMsg.Payload.ResetType;
    inOutObject->Payload.PerfDataCount = pbMsg.Payload.PerfDataCount;

	return sizeof(CFE_ES_HkPacket_t);
}


uint32 CFE_ES_PerfStopCmd_t_Enc(const CFE_ES_PerfStopCmd_t *inObject, char *inOutBuffer, uint32 inSize)
{
	bool status = false;
	pb_ostream_t stream;

	es_perf_stop_pb pbMsg;

    strcpy(pbMsg.Payload.DataFileName, inObject->Payload.DataFileName);

	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)inOutBuffer, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_perf_stop_pb_fields, &pbMsg);

	/* Check for errors... */
	if (!status)
	{
        OS_printf("Error encoding msg: %s", PB_GET_ERROR(&stream));
		return 0;
	}

	return stream.bytes_written;
}

uint32 CFE_ES_PerfStopCmd_t_Dec(const char *inBuffer, uint32 inSize, CFE_ES_PerfStopCmd_t *inOutObject)
{
	bool status = false;
	pb_istream_t stream;

	es_perf_stop_pb pbMsg;

    /* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)inBuffer, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_perf_stop_pb_fields, &pbMsg); 

	/* Check for errors... */
	if (!status)
	{
        OS_printf("Error decoding msg: %s", PB_GET_ERROR(&stream));
		return 0;
	}

    strcpy(inOutObject->Payload.DataFileName, pbMsg.Payload.DataFileName);

	return sizeof(CFE_ES_PerfStopCmd_t);
}


uint32 CFE_ES_WriteSyslogCmd_t_Enc(const CFE_ES_WriteSyslogCmd_t *inObject, char *inOutBuffer, uint32 inSize)
{
	bool status = false;
	pb_ostream_t stream;

	es_write_sys_log_pb pbMsg;

    strcpy(pbMsg.Payload.SysLogFileName, inObject->Payload.SysLogFileName);

	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)inOutBuffer, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_write_sys_log_pb_fields, &pbMsg);

	/* Check for errors... */
	if (!status)
	{
        OS_printf("Error encoding msg: %s", PB_GET_ERROR(&stream));
		return 0;
	}

	return stream.bytes_written;
}

uint32 CFE_ES_WriteSyslogCmd_t_Dec(const char *inBuffer, uint32 inSize, CFE_ES_WriteSyslogCmd_t *inOutObject)
{
	bool status = false;
	pb_istream_t stream;

	es_write_sys_log_pb pbMsg;

    /* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)inBuffer, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_write_sys_log_pb_fields, &pbMsg); 

	/* Check for errors... */
	if (!status)
	{
        OS_printf("Error decoding msg: %s", PB_GET_ERROR(&stream));
		return 0;
	}

    strcpy(inOutObject->Payload.SysLogFileName, pbMsg.Payload.SysLogFileName);

	return sizeof(CFE_ES_WriteSyslogCmd_t);
}


uint32 CFE_ES_WriteERlogCmd_t_Enc(const CFE_ES_WriteERlogCmd_t *inObject, char *inOutBuffer, uint32 inSize)
{
	bool status = false;
	pb_ostream_t stream;

	es_write_er_log_pb pbMsg;

    strcpy(pbMsg.Payload.ERLogFileName, inObject->Payload.ERLogFileName);

	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)inOutBuffer, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_write_er_log_pb_fields, &pbMsg);

	/* Check for errors... */
	if (!status)
	{
        OS_printf("Error encoding msg: %s", PB_GET_ERROR(&stream));
		return 0;
	}

	return stream.bytes_written;
}

uint32 CFE_ES_WriteERlogCmd_t_Dec(const char *inBuffer, uint32 inSize, CFE_ES_WriteERlogCmd_t *inOutObject)
{
	bool status = false;
	pb_istream_t stream;

	es_write_er_log_pb pbMsg;

    /* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)inBuffer, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_write_er_log_pb_fields, &pbMsg); 

	/* Check for errors... */
	if (!status)
	{
        OS_printf("Error decoding msg: %s", PB_GET_ERROR(&stream));
		return 0;
	}

    strcpy(inOutObject->Payload.ERLogFileName, pbMsg.Payload.ERLogFileName);

	return sizeof(CFE_ES_WriteERlogCmd_t);
}


uint32 CFE_ES_QueryAllCmd_t_Enc(const CFE_ES_QueryAllCmd_t *inObject, char *inOutBuffer, uint32 inSize)
{
	bool status = false;
	pb_ostream_t stream;

	es_query_all_pb pbMsg;

    strcpy(pbMsg.Payload.QueryAllFileName, inObject->Payload.QueryAllFileName);

	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)inOutBuffer, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_query_all_pb_fields, &pbMsg);

	/* Check for errors... */
	if (!status)
	{
        OS_printf("Error encoding msg: %s", PB_GET_ERROR(&stream));
		return 0;
	}

	return stream.bytes_written;
}

uint32 CFE_ES_QueryAllCmd_t_Dec(const char *inBuffer, uint32 inSize, CFE_ES_QueryAllCmd_t *inOutObject)
{
	bool status = false;
	pb_istream_t stream;

	es_query_all_pb pbMsg;

    /* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)inBuffer, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_query_all_pb_fields, &pbMsg); 

	/* Check for errors... */
	if (!status)
	{
        OS_printf("Error decoding msg: %s", PB_GET_ERROR(&stream));
		return 0;
	}

    strcpy(inOutObject->Payload.QueryAllFileName, pbMsg.Payload.QueryAllFileName);

	return sizeof(CFE_ES_QueryAllCmd_t);
}


uint32 CFE_ES_OneAppTlm_t_Enc(const CFE_ES_OneAppTlm_t *inObject, char *inOutBuffer, uint32 inSize)
{
	bool status = false;
	pb_ostream_t stream;

	es_one_app_pb pbMsg;

    pbMsg.Payload.AppInfo.ExecutionCounter = inObject->Payload.AppInfo.ExecutionCounter;
    pbMsg.Payload.AppInfo.NumOfChildTasks = inObject->Payload.AppInfo.NumOfChildTasks;
    strcpy(pbMsg.Payload.AppInfo.Name, inObject->Payload.AppInfo.Name);
    strcpy(pbMsg.Payload.AppInfo.MainTaskName, inObject->Payload.AppInfo.MainTaskName);
    pbMsg.Payload.AppInfo.CodeAddress = inObject->Payload.AppInfo.CodeAddress;
    pbMsg.Payload.AppInfo.MainTaskId = inObject->Payload.AppInfo.MainTaskId;
    pbMsg.Payload.AppInfo.CodeSize = inObject->Payload.AppInfo.CodeSize;
    strcpy(pbMsg.Payload.AppInfo.FileName, inObject->Payload.AppInfo.FileName);
    pbMsg.Payload.AppInfo.Priority = inObject->Payload.AppInfo.Priority;
    pbMsg.Payload.AppInfo.StartAddress = inObject->Payload.AppInfo.StartAddress;
    pbMsg.Payload.AppInfo.DataAddress = inObject->Payload.AppInfo.DataAddress;
    strcpy(pbMsg.Payload.AppInfo.EntryPoint, inObject->Payload.AppInfo.EntryPoint);
    pbMsg.Payload.AppInfo.BSSAddress = inObject->Payload.AppInfo.BSSAddress;
    pbMsg.Payload.AppInfo.AppId = inObject->Payload.AppInfo.AppId;
    pbMsg.Payload.AppInfo.BSSSize = inObject->Payload.AppInfo.BSSSize;
    pbMsg.Payload.AppInfo.AddressesAreValid = inObject->Payload.AppInfo.AddressesAreValid;
    pbMsg.Payload.AppInfo.StackSize = inObject->Payload.AppInfo.StackSize;
    pbMsg.Payload.AppInfo.DataSize = inObject->Payload.AppInfo.DataSize;
    pbMsg.Payload.AppInfo.Type = inObject->Payload.AppInfo.Type;
    pbMsg.Payload.AppInfo.ExceptionAction = inObject->Payload.AppInfo.ExceptionAction;
    pbMsg.Payload.AppInfo.ModuleId = inObject->Payload.AppInfo.ModuleId;

	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)inOutBuffer, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_one_app_pb_fields, &pbMsg);

	/* Check for errors... */
	if (!status)
	{
        OS_printf("Error encoding msg: %s", PB_GET_ERROR(&stream));
		return 0;
	}

	return stream.bytes_written;
}

uint32 CFE_ES_OneAppTlm_t_Dec(const char *inBuffer, uint32 inSize, CFE_ES_OneAppTlm_t *inOutObject)
{
	bool status = false;
	pb_istream_t stream;

	es_one_app_pb pbMsg;

    /* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)inBuffer, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_one_app_pb_fields, &pbMsg); 

	/* Check for errors... */
	if (!status)
	{
        OS_printf("Error decoding msg: %s", PB_GET_ERROR(&stream));
		return 0;
	}

    inOutObject->Payload.AppInfo.ExecutionCounter = pbMsg.Payload.AppInfo.ExecutionCounter;
    inOutObject->Payload.AppInfo.NumOfChildTasks = pbMsg.Payload.AppInfo.NumOfChildTasks;
    strcpy(inOutObject->Payload.AppInfo.Name, pbMsg.Payload.AppInfo.Name);
    strcpy(inOutObject->Payload.AppInfo.MainTaskName, pbMsg.Payload.AppInfo.MainTaskName);
    inOutObject->Payload.AppInfo.CodeAddress = pbMsg.Payload.AppInfo.CodeAddress;
    inOutObject->Payload.AppInfo.MainTaskId = pbMsg.Payload.AppInfo.MainTaskId;
    inOutObject->Payload.AppInfo.CodeSize = pbMsg.Payload.AppInfo.CodeSize;
    strcpy(inOutObject->Payload.AppInfo.FileName, pbMsg.Payload.AppInfo.FileName);
    inOutObject->Payload.AppInfo.Priority = pbMsg.Payload.AppInfo.Priority;
    inOutObject->Payload.AppInfo.StartAddress = pbMsg.Payload.AppInfo.StartAddress;
    inOutObject->Payload.AppInfo.DataAddress = pbMsg.Payload.AppInfo.DataAddress;
    strcpy(inOutObject->Payload.AppInfo.EntryPoint, pbMsg.Payload.AppInfo.EntryPoint);
    inOutObject->Payload.AppInfo.BSSAddress = pbMsg.Payload.AppInfo.BSSAddress;
    inOutObject->Payload.AppInfo.AppId = pbMsg.Payload.AppInfo.AppId;
    inOutObject->Payload.AppInfo.BSSSize = pbMsg.Payload.AppInfo.BSSSize;
    inOutObject->Payload.AppInfo.AddressesAreValid = pbMsg.Payload.AppInfo.AddressesAreValid;
    inOutObject->Payload.AppInfo.StackSize = pbMsg.Payload.AppInfo.StackSize;
    inOutObject->Payload.AppInfo.DataSize = pbMsg.Payload.AppInfo.DataSize;
    inOutObject->Payload.AppInfo.Type = pbMsg.Payload.AppInfo.Type;
    inOutObject->Payload.AppInfo.ExceptionAction = pbMsg.Payload.AppInfo.ExceptionAction;
    inOutObject->Payload.AppInfo.ModuleId = pbMsg.Payload.AppInfo.ModuleId;

	return sizeof(CFE_ES_OneAppTlm_t);
}


uint32 CFE_ES_ShellPacket_t_Enc(const CFE_ES_ShellPacket_t *inObject, char *inOutBuffer, uint32 inSize)
{
	bool status = false;
	pb_ostream_t stream;

	es_shell_packet_pb pbMsg;

    strcpy(pbMsg.Payload.ShellOutput, inObject->Payload.ShellOutput);

	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)inOutBuffer, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_shell_packet_pb_fields, &pbMsg);

	/* Check for errors... */
	if (!status)
	{
        OS_printf("Error encoding msg: %s", PB_GET_ERROR(&stream));
		return 0;
	}

	return stream.bytes_written;
}

uint32 CFE_ES_ShellPacket_t_Dec(const char *inBuffer, uint32 inSize, CFE_ES_ShellPacket_t *inOutObject)
{
	bool status = false;
	pb_istream_t stream;

	es_shell_packet_pb pbMsg;

    /* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)inBuffer, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_shell_packet_pb_fields, &pbMsg); 

	/* Check for errors... */
	if (!status)
	{
        OS_printf("Error decoding msg: %s", PB_GET_ERROR(&stream));
		return 0;
	}

    strcpy(inOutObject->Payload.ShellOutput, pbMsg.Payload.ShellOutput);

	return sizeof(CFE_ES_ShellPacket_t);
}


uint32 CFE_ES_RestartCmd_t_Enc(const CFE_ES_RestartCmd_t *inObject, char *inOutBuffer, uint32 inSize)
{
	bool status = false;
	pb_ostream_t stream;

	es_restart_pb pbMsg;

    pbMsg.Payload.RestartType = inObject->Payload.RestartType;

	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)inOutBuffer, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_restart_pb_fields, &pbMsg);

	/* Check for errors... */
	if (!status)
	{
        OS_printf("Error encoding msg: %s", PB_GET_ERROR(&stream));
		return 0;
	}

	return stream.bytes_written;
}

uint32 CFE_ES_RestartCmd_t_Dec(const char *inBuffer, uint32 inSize, CFE_ES_RestartCmd_t *inOutObject)
{
	bool status = false;
	pb_istream_t stream;

	es_restart_pb pbMsg;

    /* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)inBuffer, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_restart_pb_fields, &pbMsg); 

	/* Check for errors... */
	if (!status)
	{
        OS_printf("Error decoding msg: %s", PB_GET_ERROR(&stream));
		return 0;
	}

    inOutObject->Payload.RestartType = pbMsg.Payload.RestartType;

	return sizeof(CFE_ES_RestartCmd_t);
}


uint32 CFE_ES_OverWriteSysLogCmd_t_Enc(const CFE_ES_OverWriteSysLogCmd_t *inObject, char *inOutBuffer, uint32 inSize)
{
	bool status = false;
	pb_ostream_t stream;

	es_overwrite_sys_log_pb pbMsg;

    pbMsg.Payload.Mode = inObject->Payload.Mode;

	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)inOutBuffer, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_overwrite_sys_log_pb_fields, &pbMsg);

	/* Check for errors... */
	if (!status)
	{
        OS_printf("Error encoding msg: %s", PB_GET_ERROR(&stream));
		return 0;
	}

	return stream.bytes_written;
}

uint32 CFE_ES_OverWriteSysLogCmd_t_Dec(const char *inBuffer, uint32 inSize, CFE_ES_OverWriteSysLogCmd_t *inOutObject)
{
	bool status = false;
	pb_istream_t stream;

	es_overwrite_sys_log_pb pbMsg;

    /* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)inBuffer, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_overwrite_sys_log_pb_fields, &pbMsg); 

	/* Check for errors... */
	if (!status)
	{
        OS_printf("Error decoding msg: %s", PB_GET_ERROR(&stream));
		return 0;
	}

    inOutObject->Payload.Mode = pbMsg.Payload.Mode;

	return sizeof(CFE_ES_OverWriteSysLogCmd_t);
}


uint32 CFE_ES_PerfSetFilterMaskCmd_t_Enc(const CFE_ES_PerfSetFilterMaskCmd_t *inObject, char *inOutBuffer, uint32 inSize)
{
	bool status = false;
	pb_ostream_t stream;

	es_perf_set_filter_mask_pb pbMsg;

    pbMsg.Payload.FilterMaskNum = inObject->Payload.FilterMaskNum;
    pbMsg.Payload.FilterMask = inObject->Payload.FilterMask;

	/* Create a stream that will write to our buffer. */
	stream = pb_ostream_from_buffer((pb_byte_t *)inOutBuffer, inSize);
	
	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_perf_set_filter_mask_pb_fields, &pbMsg);

	/* Check for errors... */
	if (!status)
	{
        OS_printf("Error encoding msg: %s", PB_GET_ERROR(&stream));
		return 0;
	}

	return stream.bytes_written;
}

uint32 CFE_ES_PerfSetFilterMaskCmd_t_Dec(const char *inBuffer, uint32 inSize, CFE_ES_PerfSetFilterMaskCmd_t *inOutObject)
{
	bool status = false;
	pb_istream_t stream;

	es_perf_set_filter_mask_pb pbMsg;

    /* Create a stream that reads from the buffer. */
	stream = pb_istream_from_buffer((const pb_byte_t *)inBuffer, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_perf_set_filter_mask_pb_fields, &pbMsg); 

	/* Check for errors... */
	if (!status)
	{
        OS_printf("Error decoding msg: %s", PB_GET_ERROR(&stream));
		return 0;
	}

    inOutObject->Payload.FilterMaskNum = pbMsg.Payload.FilterMaskNum;
    inOutObject->Payload.FilterMask = pbMsg.Payload.FilterMask;

	return sizeof(CFE_ES_PerfSetFilterMaskCmd_t);
}


#ifdef __cplusplus
} /* extern "C" */
#endif
