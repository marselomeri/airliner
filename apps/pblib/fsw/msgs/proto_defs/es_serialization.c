/* This file was autogenerated from template version 0.0.0.1 and autogenerator version 0.0.0.1 */

#include "es_serialization.h"

#ifdef __cplusplus
extern "C" {
#endif

uint32 ES_RegisterSerializationFuncs()
{
	int32 Status = CFE_SUCCESS;

	/* Register each message with PBL */
	Status = PBLIB_RegisterMessage(0x1806, 14, "CFE_ES_PerfStartCmd_t");
	if (Status != CFE_SUCCESS)
	{
		goto ES_RegisterSerializationFuncs_Exit_Tag;
	}
	
	Status = PBLIB_RegisterMessage(0x1806, 2, "CFE_ES_RestartCmd_t");
	if (Status != CFE_SUCCESS)
	{
		goto ES_RegisterSerializationFuncs_Exit_Tag;
	}
	
	Status = PBLIB_RegisterMessage(0x1806, 16, "CFE_ES_PerfSetFilterMaskCmd_t");
	if (Status != CFE_SUCCESS)
	{
		goto ES_RegisterSerializationFuncs_Exit_Tag;
	}
	
	Status = PBLIB_RegisterMessage(0x1806, 22, "CFE_ES_PoolStatsTlm_t");
	if (Status != CFE_SUCCESS)
	{
		goto ES_RegisterSerializationFuncs_Exit_Tag;
	}
	
	Status = PBLIB_RegisterMessage(0x1806, 3, "CFE_ES_ShellCmd_t");
	if (Status != CFE_SUCCESS)
	{
		goto ES_RegisterSerializationFuncs_Exit_Tag;
	}
	
	Status = PBLIB_RegisterMessage(0x1806, 15, "CFE_ES_PerfStopCmd_t");
	if (Status != CFE_SUCCESS)
	{
		goto ES_RegisterSerializationFuncs_Exit_Tag;
	}
	
	Status = PBLIB_RegisterMessage(0x1806, 99, "CFE_ES_ShellPacket_t");
	if (Status != CFE_SUCCESS)
	{
		goto ES_RegisterSerializationFuncs_Exit_Tag;
	}
	
	Status = PBLIB_RegisterMessage(0x1806, 18, "CFE_ES_OverWriteSysLogCmd_t");
	if (Status != CFE_SUCCESS)
	{
		goto ES_RegisterSerializationFuncs_Exit_Tag;
	}
	
	Status = PBLIB_RegisterMessage(0x1806, 9, "CFE_ES_QueryAllCmd_t");
	if (Status != CFE_SUCCESS)
	{
		goto ES_RegisterSerializationFuncs_Exit_Tag;
	}
	
	Status = PBLIB_RegisterMessage(0x1806, 17, "CFE_ES_PerfSetTrigMaskCmd_t");
	if (Status != CFE_SUCCESS)
	{
		goto ES_RegisterSerializationFuncs_Exit_Tag;
	}
	
	Status = PBLIB_RegisterMessage(0x1806, 7, "CFE_ES_AppReloadCmd_t");
	if (Status != CFE_SUCCESS)
	{
		goto ES_RegisterSerializationFuncs_Exit_Tag;
	}
	
	Status = PBLIB_RegisterMessage(0x1806, 13, "CFE_ES_WriteERlogCmd_t");
	if (Status != CFE_SUCCESS)
	{
		goto ES_RegisterSerializationFuncs_Exit_Tag;
	}
	
	Status = PBLIB_RegisterMessage(0x1806, 5, "CFE_ES_AppNameCmd_t");
	if (Status != CFE_SUCCESS)
	{
		goto ES_RegisterSerializationFuncs_Exit_Tag;
	}
	
	Status = PBLIB_RegisterMessage(0x0800, 0, "CFE_ES_HkPacket_t");
	if (Status != CFE_SUCCESS)
	{
		goto ES_RegisterSerializationFuncs_Exit_Tag;
	}
	
	Status = PBLIB_RegisterMessage(0x1806, 8, "CFE_ES_OneAppTlm_t");
	if (Status != CFE_SUCCESS)
	{
		goto ES_RegisterSerializationFuncs_Exit_Tag;
	}
	
	Status = PBLIB_RegisterMessage(0x1806, 20, "CFE_ES_SetMaxPRCountCmd_t");
	if (Status != CFE_SUCCESS)
	{
		goto ES_RegisterSerializationFuncs_Exit_Tag;
	}
	
	Status = PBLIB_RegisterMessage(0x1806, 4, "CFE_ES_StartAppCmd_t");
	if (Status != CFE_SUCCESS)
	{
		goto ES_RegisterSerializationFuncs_Exit_Tag;
	}
	
	Status = PBLIB_RegisterMessage(0x1806, 21, "CFE_ES_DeleteCDSCmd_t");
	if (Status != CFE_SUCCESS)
	{
		goto ES_RegisterSerializationFuncs_Exit_Tag;
	}
	
	Status = PBLIB_RegisterMessage(0x1806, 24, "CFE_ES_QueryAllTasksCmd_t");
	if (Status != CFE_SUCCESS)
	{
		goto ES_RegisterSerializationFuncs_Exit_Tag;
	}
	
	Status = PBLIB_RegisterMessage(0x1806, 11, "CFE_ES_WriteSyslogCmd_t");
	if (Status != CFE_SUCCESS)
	{
		goto ES_RegisterSerializationFuncs_Exit_Tag;
	}
	
	Status = PBLIB_RegisterMessage(0x1806, 22, "CFE_ES_TlmPoolStatsCmd_t");
	if (Status != CFE_SUCCESS)
	{
		goto ES_RegisterSerializationFuncs_Exit_Tag;
	}
	
	Status = PBLIB_RegisterMessage(0x1806, 23, "CFE_ES_DumpCDSRegCmd_t");
	if (Status != CFE_SUCCESS)
	{
		goto ES_RegisterSerializationFuncs_Exit_Tag;
	}
	
ES_RegisterSerializationFuncs_Exit_Tag:
    return Status;
}

uint32 CFE_ES_PerfStartCmd_t_Enc(const CFE_ES_PerfStartCmd_t *inObject, char *inOutBuffer, uint32 inSize)
{
	bool status = false;
	es_perf_start_pb pbMsg;
	
	//pbMsg.payload = inObject->Payload;

	/* Create a stream that will write to our buffer. */
	pb_ostream_t stream = pb_ostream_from_buffer((pb_byte_t *)inOutBuffer, inSize);

	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_perf_start_pb_fields, &pbMsg); 

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	return stream.bytes_written;
}

uint32 CFE_ES_PerfStartCmd_t_Dec(const char *inBuffer, uint32 inSize, CFE_ES_PerfStartCmd_t *inOutObject)
{
	bool 							status = false;
	es_perf_start_pb 				pbMsg;
	es_perf_start_payload_pb 		payloadPbMsg;
	CFE_ES_PerfStartCmd_Payload_t 	Payload;

	/* Create a stream that reads from the buffer. */
	pb_istream_t stream = pb_istream_from_buffer((const pb_byte_t *)inBuffer, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_perf_start_pb_fields, &pbMsg); 

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	/* Decode the payload */
	status = pb_decode((pb_istream_t *)&inOutObject->Payload, es_perf_start_payload_pb_fields, &payloadPbMsg); 

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	Payload.TriggerMode = payloadPbMsg.TriggerMode;
	
	inOutObject->Payload = Payload;

	return sizeof(CFE_ES_PerfStartCmd_t);
}

uint32 CFE_ES_PerfStartCmd_Payload_t_Enc(const CFE_ES_PerfStartCmd_Payload_t *inObject, char *inOutBuffer, uint32 inSize)
{
	bool status = false;
	es_perf_start_pb pbMsg;
	
	//pbMsg.TriggerMode = inObject->TriggerMode;

	/* Create a stream that will write to our buffer. */
	pb_ostream_t stream = pb_ostream_from_buffer((pb_byte_t *)inOutBuffer, inSize);

	/* Now we are ready to encode the message. */
	status = pb_encode(&stream, es_perf_start_pb_fields, &pbMsg); 

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}

	return stream.bytes_written;
}

uint32 CFE_ES_PerfStartCmd_Payload_t_Dec(const char *inBuffer, uint32 inSize, CFE_ES_PerfStartCmd_Payload_t *inOutObject)
{
	bool status = false;
	es_perf_start_payload_pb pbMsg;

	/* Create a stream that reads from the buffer. */
	pb_istream_t stream = pb_istream_from_buffer((const pb_byte_t *)inBuffer, inSize);

	/* Now we are ready to decode the message. */
	status = pb_decode(&stream, es_perf_start_pb_fields, &pbMsg); 

	/* Check for errors... */
	if (!status)
	{
		return 0;
	}
	
	inOutObject->TriggerMode = pbMsg.TriggerMode;

	return sizeof(CFE_ES_PerfStartCmd_Payload_t);
}




#ifdef __cplusplus
} /* extern "C" */
#endif
