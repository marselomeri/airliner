syntax = "proto2";
import "nanopb.proto";

message ea_start_pb
{
	required string interpreter = 1 [(nanopb).max_size = 64];
	required string script = 2 [(nanopb).max_size = 64];
}

// ORB_DECLARE(sensor_accel);
