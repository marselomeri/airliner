syntax = "proto2";
import "nanopb.proto";

message px4_differential_pressure_pb
{
	required uint64 timestamp = 1;
	required uint64 error_count = 2;
	required float differential_pressure_raw_pa = 3;
	required float differential_pressure_filtered_pa = 4;
	required float temperature = 5;
}

// ORB_DECLARE(differential_pressure);
