syntax = "proto2";
import "nanopb.proto";

message px4_pwm_input_pb 
{
	required uint64 timestamp = 1;
	required uint64 error_count = 2;
	required uint32 pulse_width = 3;
	required uint32 period = 4;
}

// ORB_DECLARE(pwm_input);
