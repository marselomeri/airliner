syntax = "proto2";
import "nanopb.proto";

message px4_safety_pb 
{
	required uint64 timestamp = 1;
	required bool safety_switch_available = 2;
	required bool safety_off = 3;
}

// ORB_DECLARE(safety);
