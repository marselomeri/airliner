syntax = "proto2";
import "nanopb.proto";

message px4_gps_inject_data_pb 
{
	required uint64 timestamp = 1;
	required uint32 len = 2;
	required uint32 flags = 3;
	required string data = 4 [(nanopb).max_size = 182];
}

// ORB_DECLARE(gps_inject_data);
