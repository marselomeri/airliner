syntax = "proto2";
import "nanopb.proto";

message px4_log_message_pb 
{
	required uint64 timestamp = 1;
	required uint32 severity = 2;
	required string text = 3 [(nanopb).max_size = 127];
}

// ORB_DECLARE(log_message);
