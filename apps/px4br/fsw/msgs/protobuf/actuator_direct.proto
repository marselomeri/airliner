syntax = "proto2";
import "nanopb.proto";

message px4br_actuator_direct_pb {
	enum ACTUATOR_DIRECT
	{
		ACTUATOR_DIRECT_NUM_ACTUATORS_DIRECT = 16;
	}

	required uint64 timestamp = 1;
	required uint32 nvalues = 2;
	repeated float values = 3 [packed=true, (nanopb).max_count = 16];
}	

// ORB_DECLARE(actuator_direct)
