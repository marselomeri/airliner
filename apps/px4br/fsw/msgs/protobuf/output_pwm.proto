syntax = "proto2";
import "nanopb.proto";

message px4_output_pwm_pb 
{
	enum OUTPUT_PWM
	{
		PWM_OUTPUT_MAX_CHANNELS = 16;
	}

	required uint64 timestamp = 1;
	required uint32 channel_count = 2;
	repeated uint32 values = 3 [packed=true, (nanopb).max_count = 16];
}

// ORB_DECLARE(output_pwm);
