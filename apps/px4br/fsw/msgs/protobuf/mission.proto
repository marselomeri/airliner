syntax = "proto2";
import "nanopb.proto";

message px4_mission_pb
{
	required uint64 timestamp = 1;
	required int32 dataman_id = 2;
	required uint32 count = 3;
	required int32 current_seq = 4;
}

// ORB_DECLARE(mission);
// ORB_DECLARE(offboard_mission);
// ORB_DECLARE(onboard_mission);
