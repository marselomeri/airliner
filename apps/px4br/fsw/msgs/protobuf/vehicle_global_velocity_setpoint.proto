syntax = "proto2";
import "nanopb.proto";

message px4_vehicle_global_velocity_setpoint_pb 
{
	required uint64 timestamp = 1;
	required float vx = 2;
	required float vy = 3;
	required float vz = 4;
}

// ORB_DECLARE(vehicle_global_velocity_setpoint);
