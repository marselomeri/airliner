syntax = "proto2";
import "nanopb.proto";

message px4br_qshell_req_pb 
{
	enum QSHELL_REQ
	{
		QSHELL_REQ_MAX_STRLEN = 100;
	}

	required uint64 timestamp = 1;
	required uint64 strlen = 2;
	repeated int32 string = 3 [packed=true, (nanopb).max_count = 100];
}

// ORB_DECLARE(qshell_req);
