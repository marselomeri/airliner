- queryString = '$.Airliner.apps.FM.config.FM_TABLE_ENTRY_COUNT';
-
- global.COMMANDER.queryConfigDB(queryString, function (results) {
-     FM_TABLE_ENTRY_COUNT = results[0];

table.table.table-condensed
  tbody
    tr
      th File Name
      th Upper 32 bit
      th Lower 32 bit
    - for(var i = 0; i < FM_TABLE_ENTRY_COUNT; ++i) {
    tr
      td
        span(data-cdr={tlm: [{name:'/FM/FM_FreeSpacePkt_t/FileSys[' + i + '].Name'}]}) ---
      td
        span(data-cdr={tlm: [{name:'/FM/FM_FreeSpacePkt_t/FileSys[' + i + '].FreeSpace_A'}]}) ---
      td
        span(data-cdr={tlm: [{name:'/FM/FM_FreeSpacePkt_t/FileSys[' + i + '].FreeSpace_B'}]}) ---
    - }
              
footer.btn-group(role="group")      
  button.btn.cdr-primary(data-cdr={cmd:{name:'/FM/FM_GetFreeSpaceCmd_t'}}) Get Free Space
- });
      
